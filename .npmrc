auto-install-peers = true
strict-peer-dependencies = false
prefer-workspace-packages = true
link-workspace-packages = deep
hoist = true
shamefully-hoist = true
engine-strict = false
# vendor/flowise has no .git, so its `postinstall: husky install` fails. Skip lifecycle scripts at install time.
ignore-scripts = true
