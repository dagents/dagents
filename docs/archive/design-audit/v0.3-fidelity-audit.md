# v0.3 Design-Fidelity 审计

> 任务: [MZW-286] [v0.3-M0.1] 写 design-fidelity 审计文档 (9 屏 delta + 后端契约对照)
> 上游 plan: `docs/superpowers/plans/2026-07-13-v0.3-design-fidelity-redesign.md` §Task M0.1
> 分支: `issue/MZW-286`（基于 `main`）
> 约定: design DOM/CSS 1:1 移植；交互逻辑（design 内联 `<script>`）移植为 React；每任务完成后回本文档标 ✓

本文档是 v0.3 design-fidelity 重设计的**基线审计**：把 `design/*.html` + `design/js/*` 与 `apps/console` 当前实现逐条对照，列出每屏的「design 有 / 当前缺或简化」delta，以及 3 项后端契约的字段级对照。后续每屏实现任务（M1–M9）动手前先查本文对应小节，完成后再回标 ✓。

> **收口状态（v0.3-M11.1 @`4196965`，2026-07-14）**：M1.1–M9.3 各屏保真任务均已合并入 `main`（HEAD `4196965`）。本次回填把每条 delta 的状态列标 `[✓ Mx.y @<sha>]`，并在各小节补 milestone 落地说明。状态列保留基线 emoji（❌/⚠️/✅/➕）仅为对照——**以 `[✓ …]` 为准**。仍标 ⬜/⚠️ 的项是**有意推迟**（post-MVP，见各小节说明），非遗漏。后端契约 1/2/3 的字段级 delta 表保持原样（记录契约形状差异），仅在表上方补 M9.x 落地注。
>
> ⚠️ 本计划上游 spec `docs/architecture-v0.3.md` 在仓库中**从未建稿**（plan/多处 commit 引用其 §3.1/§4/§6.8/§10.2，但文件不存在；架构叙事以 `docs/architecture-v0.2.md` + `docs/superpowers/specs/2026-07-08-mvp-execution-plan-design.md` 为准）。M11.1 的「§10.2 route 表注『已保真实现』」改记于本文末「[§10 控制台 9 屏保真状态](#10-控制台-9-屏保真状态v03-收口)」+ 新建 `docs/architecture-v0.3.md`（addendum，仅承载 §10 console 路由状态）。

图例:
- ✅ design 与 console 都有且语义一致
- ⚠️ console 有但被简化 / 语义偏离
- ❌ design 有，console 完全缺失
- ➕ console 有但 design 没有（非保真缺口，仅作背景）

源文件对照锚点（行号以 `main` HEAD `710b863` 为准）:
- design: `design/*.html` + `design/js/app.js` + `design/js/{agents,flows,lab,workspace,tokens}-data.js`
- console: `apps/console/src/app/**/page.tsx` + `apps/console/src/components/*.tsx` + `apps/console/src/lib/*.ts` + `apps/console/src/app/api/**/route.ts`

---

## 目录

- [1. agentflows](#1-agentflows)
- [2. new-task](#2-new-task)
- [3. agent-detail](#3-agent-detail)
- [4. agents](#4-agents)
- [5. index](#5-index)
- [6. workspace](#6-workspace)
- [7. lab](#7-lab)
- [8. dashboard](#8-dashboard)
- [9. settings](#9-settings)
- [后端契约 1: GET /agents/:id](#后端契约-1-get-agentsid)
- [后端契约 2: GET /chatflows/:id](#后端契约-2-get-chatflowsid)
- [后端契约 3: POST /tasks](#后端契约-3-post-tasks)
- [§10 控制台 9 屏保真状态（v0.3 收口）](#10-控制台-9-屏保真状态v03-收口)

---

## 1. agentflows

**design**: `design/agentflows.html` + `design/js/app.js` + `design/js/flows-data.js`
**console**: `apps/console/src/app/flows/page.tsx` → `apps/console/src/components/flows-view.tsx` (+ `flow-dag.tsx` / `dag-node.tsx` / `lib/flows.ts` / `app/api/flows/route.ts` / `app/api/flows/[id]/route.ts`)

| # | Delta 项 | 状态 | design 证据 (file:line) | console 证据 (file:line) | 说明 |
|---|---|---|---|---|---|
| 1.1 | scope tabs (mine / all / archived) | [✓ M2.1 @11997412] | `agentflows.html:157-161`（`.scope-tabs` 三档 + `.cnt`）；`agentflows.html:298-318` `updateScopeCounts`；过滤 `agentflows.html:281-296` | `flows-view.tsx` 渲染 `.scope-tabs` 三档 button[data-scope] + `.cnt`；`scopeCounts`/`visibleFlows` 过滤；`FlowSummary.owner`/`archived` 字段（`lib/flows.ts`） | mine 计数恒 0（Flowise 无 owner 列），tab 仍在；archived 派生自 status failed/paused |
| 1.2 | filter chips (running / done / paused / failed) | [✓ M2.1 @11997412] | `agentflows.html:168-171` 四枚 `.filter-chip[data-f="status"]`；toggle `agentflows.html:419-426`；`visibleFlows` 应用 `agentflows.html:291` | `flows-view.tsx` `.flow-toolbar` 内四枚 `.filter-chip[data-f=status][data-v=…]`，`aria-pressed` toggle + `statusFilter` Set 过滤 | 标签 1:1（运行中/已完成/已暂停/失败） |
| 1.3 | flow-card 可展开 run 历史 | [✓ M2.1 @11997412] | `agentflows.html:329-368` `.flow-card` + `.flow-card-head[data-toggle]` + 折叠 `.flow-runs`；展开 `agentflows.html:372-396`；合成 `RUN_HISTORY` `agentflows.html:245-272` | `flows-view.tsx` 渲染 `.flow-card` + `.flow-card-head[data-toggle][aria-expanded]`，点击 toggle `.expanded`；`.flow-runs` 内 run-row 列 `latestRunId` | run 历史暂只列 latest run（summary 携带的 latestRunId），历史多 run 待后端契约 |
| 1.4 | run-row showDetail | [✓ M2.1 @11997412] | `agentflows.html:358-366` `.run-row` 为 `<a href="#flow=…&run=…">`；`agentflows.html:397-405` 调 `showDetail(flowId, runId)`；`showDetail` `agentflows.html:432-462` 切换 list→detail | `flows-view.tsx` run-row 点击 → `setDetail({flowId, runId})` 切换到内联 detail（DAG + inspector，复用 M5a.3 `.flow-layout`） | URL hash deep-link 仍未实现（见 1.x） |
| 1.5 | edit 按钮 (→ /flows/[id]/edit) | [✓ M2.1 @11997412] | `agentflows.html:350` `<button data-action="edit">编辑画布</button>`；`agentflows.html:379-380` handler（占位 alert） | `flows-view.tsx` `button[data-action=edit][data-flow-id]` → `router.push('/flows/'+fid+'/edit')` | 路由本身 M2.3 落地，M2.1 仅接线导航 |
| 1.6 | run 按钮 (per flow) | [✓ M2.1 @11997412] | `agentflows.html:351` `<button data-action="run">▶ 运行</button>`；`agentflows.html:381-388` handler（找 flow 后 `showDetail`） | `flows-view.tsx` `button[data-action=run][data-flow-id]` → `setDetail({flowId, latestRunId})` showDetail | |

### 1.x other gaps (agentflows)

> M2.1 ([✓ @11997412]) 落地 list-page 主交互（1.1–1.6）。下列 1.x 次要 gap 状态更新如下；detail-page 保真（DAG 节点图标、inspector 输入/输出 io-box、URL hash deep-link）属 M2.2 范围。

- **detail 页是另一个布局形态**: [✓ M2.1 @11997412] — `flows-view.tsx` 现以 list↔detail 切页：list-page 堆叠卡片，run-row/run 按钮触发 `setDetail` 切到内联 detail（复用 M5a.3 `.flow-layout` 两栏 DAG+inspector），「返回 AgentFlows」`.flow-back` 复位。
- **「+ 新建 Flow」页操作按钮**: [✓ M2.1 @11997412] — `PageShell` 传 `actions` 渲染 `+ 新建 Flow` 按钮（design `agentflows.html:153`）。
- **search result-count**: [✓ M2.1 @11997412] — `.result-count` 显示 `${visible} / ${total} 个 flow`（design `agentflows.html:173,322`）。
- **per-flow glyph + sub-meta**: [✓ M2.1 @11997412] — `.flow-glyph` 首字 + `.sub`（id/sha/node-count/run-count）（design `agentflows.html:335-344`）。
- **active run chip**: [✓ M2.1 @11997412] — 卡片 `.flow-card-meta` 挂 `latestRunId` chip（design `agentflows.html:347`）。
- **URL hash deep-link**: ⬜ 仍缺 — `detail` 是 React state，不可寻址；M2.2 改。
- **DAG 节点类型图标**: ⬜ 仍缺 — `dag-node.tsx` 仍 label 文本；M2.2 改。
- **inspector 区段（输入/输出/预算上限/调用 agent/超时）**: ⬜ 仍缺 — NodeInspector 仍无输入/输出 io-box；M2.2 改。
- **legend「未触发」标签**: ✅ 一致。

**→ 对应里程碑**: M2.1–M2.4（agentflows + edit canvas）

> [✓ M2.2 @3a0c24f] — list↔detail swap + DAG + inspector io-box 已落地（`flows-view.tsx` 顶层 `showDetail(flowId,runId)`/`hideDetail` 切页、`.flow-detail-page.active` + `.flow-back` 返回、`#flow=&run=` hash deep-link、`FlowDag` 渲染、`NodeInspector` 补 输入/输出 io-box + 预算上限/调用 agent/超时）。rebase 到含 M2.1/M2.3 的 main 后，list-page 的 scope tabs / filter chips / edit+run action buttons（M2.1）已与 detail swap 同文件共存。

---

## 2. new-task

**design**: `design/new-task.html` + `design/js/app.js`
**console**: **无 new-task 页 / 组件 / API 路由**（`apps/console/src/app/` 仅 agents/api/dashboard/flows/lab/login/settings/workspace；无 `new-task/` 或 `tasks/`；grep `new-task|create-task|newtask` 零命中；`nav.ts` 无 new-task 条目；全仓无 `webkitdirectory`/`directory` 属性）

### 2.1 design specifies

整体创建流程（`design/new-task.html`）：一个垂直居中 composer（`.nt-top`/`.nt-bottom`，CSS L21-42；布局注释 L11-13），自上而下：目录 workspace 卡片 (`.nt-ws-card` L311) → composer (`.nt-composer` L326，含 `.nt-assoc` 关联 chip rail L327 + `#nt-textarea` L330 placeholder「描述你要完成的任务…」 + `.nt-foot` L333 关联按钮 + 键盘提示 + `.nt-send` L360 label「创建并派发」) → 空状态建议 (`.nt-suggest` L370，4 模板 L410-415：论文复现/假设验证/代码迁移/跨域综述)。

提交行为（`doSend` `design/new-task.html:564-574`）：构造 `URLSearchParams`（`task`/`dir`/`flows`/`agents`），然后 **`location.href = 'workspace.html?new=1&' + params`**（L573）——**没有 fetch/POST**，是客户端跳转到 Workspace 页通过 query string 交接；目标「endpoint」是 Workspace 路由而非 tasks API。

| # | Delta 项 | 状态 | design 证据 (file:line) | 说明 |
|---|---|---|---|---|
| 2.1 | nt-picker 模态 | [✓ M3.1 @5e1c6f7] | CSS `new-task.html:219-270`；markup `new-task.html:338-356`；JS `new-task.html:461-546` | 实为 **popover**（`position:absolute; bottom:calc(100% + 8px); width:340px` L221-224）+ backdrop (`new-task.html:377`)。**两个独立 picker**：`#nt-picker-flow`「选择 AgentFlow」(L338) 与 `#nt-picker-agent`「选择 Agent」(L350)，各含搜索框 (`.nt-picker-search` L339-340/351-352) + 可滚选项列表 (`.nt-picker-list` max-h 280px L245) + 选项 (`.nt-picker-opt` L246-269 含 glyph/name/meta/选中勾)。**多选**：切换增删 `.assoc-chip` 到 composer rail (`new-task.html:481-517`)。Esc/backdrop/外点关闭 (`new-task.html:542-546`)。picker 选的是「与该 task 关联的 AgentFlow 与 Agent」。**M3.1** 落地为 `apps/console/src/app/tasks/new/page.tsx` + `components/new-task-view.tsx`：两个 popover picker（关联 Flow / 关联 Agent）+ 搜索框 + 选项列表 + 多选 assoc-chip rail + Esc/backdrop/外点关闭。Flow 选项源 `/api/flows`（→ gateway → Flowise AGENTFLOW），Agent 选项源 `/api/agents`（→ dispatch）。回归修复见 `aeb21bc`（线形契约 + 嵌套交互 + backdrop）。测试 `picker.test.tsx` |
| 2.2 | ⏎⇧⏎ 键盘快捷键 | [✓ M3.2 @47927d1] | 提示 `new-task.html:359` `⏎ 发送 · ⇧⏎ 换行`；实现 `new-task.html:559-561` | Enter = 发送（提交/创建 task）；Shift+Enter = 换行（在 textarea 插入换行）；send 按钮也可点 (`new-task.html:562`)。**M3.2** 落地为 `onKeyDownTextarea`（`new-task-view.tsx`）：Enter(no Shift) → `preventDefault` + `doSend()`；Shift+Enter 放行原生 `\n`。比 design 多一个 IME 守卫（`!isComposing`，CJK 候选窗打开时 Enter 确认候选而非提交）。测试 `keyboard.test.tsx` |
| 2.3 | nt-ws-card 目录选择 | [✓ M3.3] | CSS `new-task.html:43-90`；markup `new-task.html:311-322`；JS `new-task.html:417-459` | 虚线边框卡片按钮 (L311, `aria-label="选择本地目录作为 workspace"`)，点击 (`new-task.html:425`) 触发隐藏 `<input type="file" id="nt-dir-input" webkitdirectory directory multiple hidden />` (L322)。`change` (`new-task.html:426-437`) 从 `files[0].webkitRelativePath` 重建目录路径，存 `state.dir={name,path,count}`，`renderDir()` (`new-task.html:438-453`) 切到 `.has-dir` 样式（实线 + warm surface），显示目录名为 title、`path · ' count ' 个文件已索引` 为副标题，露出 clear 按钮 (`.nt-ws-clear` L318)；clear (`new-task.html:454-459`) 复位。**M3.3**: 卡片点击优先 `window.showDirectoryPicker()`（File System Access API，Chromium 可用），DFS 遍历 `handle.values()` 把每个文件收成 `contextRef`；API 缺失时回退到 `webkitdirectory` input，`change` 用每个 File 的 `webkitRelativePath` 构建 `contextRefs`。两条路径收敛到同一 `DirState{name,path,count,contextRefs}`，submit 在 `/workspace?new=1` 交接 query 里加 `contextRefs=[{path}]` (JSON)。测试 `ws-card.test.tsx`（6 例：FS Access 深层遍历/Abort/回退/clear）。`tsconfig.json` 增 `dom.asynciterable` lib 以解 `FileSystemDirectoryHandle.values()` async 迭代器类型 |

### 2.2 console status

> **[✓ M3.1 @5e1c6f7] / [✓ M3.2 @47927d1] / [✓ M3.3 @4f6e093]** new-task 页已落地：`apps/console/src/app/tasks/new/page.tsx`（路由 `tasks/new`）→ `components/new-task-view.tsx`，含 picker 模态、目录卡、composer、建议模板。`nav.ts` 编排区「新增 Task」plus 按钮指向本路由。`webkitdirectory` + File System Access API 原语已引入（`dom.asynciterable` lib）。下方 baseline 描述为审计建稿时（HEAD `710b863`）的 MISSING 状态，保留以示对照。

**MISSING** —— 无 new-task 页、组件、nav 条目、API 路由。

- `apps/console/src/app/` 下无 `new-task/`、`tasks/`、`create-task/` 目录。
- grep `new-task|new_task|create-task|create_task|newtask|CreateTask|新增 task|新增task` 跨 `apps/console/src/` **零命中**。
- `apps/console/src/components/nav.ts` 无 new-task 条目；design sidebar (`design/js/app.js:69-76`) 在 Workspace 段头有 `nav-add-task` plus 按钮 → `new-task.html`，console `NAV` 数组无对应。
- 全仓无 `webkitdirectory`/`directory` 属性，目录选择原语不存在。
- 唯一「沾边」的是 workspace 线程内 composer（`apps/console/src/components/workspace-view.tsx:436-447`，send 回调 `workspace-view.tsx:231-285` 走 `postWorkspaceRun` POST `/api/workspaces/:id/runs`）——这是给**已存在** workspace 线程追加一回合，不是独立的 new-task 创建页：无目录 picker、无 flow/agent 关联 picker、无 ⏎⇧⏎ 提示、无建议模板。

**→ 对应里程碑**: M3（new-task）

---

## 3. agent-detail

**design**: `design/agent-detail.html` + `design/js/app.js` + `design/js/agents-data.js`
**console**: **无 agent-detail 页/动态路由**（`apps/console/src/app/agents/` 仅 `page.tsx` 列表页，无 `[id]/` 子目录、无 `app/agents/[id]/page.tsx`）；部分功能以**滑出 drawer** 形式存在于 `apps/console/src/components/agents-view.tsx`，但无两栏 split、无 tabs、无 30 桶 activity 图。

### 3.1 design specifies

整体布局（`design/agent-detail.html`）：返回链接 → agents.html (`agent-detail.html:111`)；`.detail-layout` (`agent-detail.html:117`) = CSS grid `grid-template-columns: 320px minmax(0,1fr)` (`agent-detail.html:15`)。左列 `.inspector` 320px sticky (`agent-detail.html:18`)；右列 `.overview` (`agent-detail.html:42`)。数据源：**无 fetch**，读 `window.OD_AGENTS`（`design/js/agents-data.js`，于 `agent-detail.html:147` 加载）按 `URLSearchParams(location.search).get('id')` 取 agent（`agent-detail.html:156`，默认 `'agent_01HFK'`），200ms 骨架屏 (`agent-detail.html:347-350`) 后 `render(a)`；找不到则 `renderNotFound()` (`agent-detail.html:172-179`)。agent 形状（`agents-data.js:27-43` 每条）：`id, name, kind, roles, status, availability, region, daemon, run, flow, load, cost, progress, elapsed, inputSchema, outputSchema, summary, instructions, skills, visibility, concurrency, model, runtime, owner, created, activity(30 buckets), logs`。

| # | Delta 项 | 状态 | design 证据 (file:line) | 说明 |
|---|---|---|---|---|
| 3.1 | 左 inspector / 右 overview tabs | [✓ M4.1 @ac2927f] | grid `agent-detail.html:15, 117-139`；左 inspector `.inspector` (`agent-detail.html:119-124`，`renderInspector` `agent-detail.html:181-217`)：ins-head(kind 着色 avatar + name + summary + availability 在线状态 `agent-detail.html:185-192`)；属性段 prop-rows（Agent ID/类型/模型/运行时/并发/可见性/负责人/创建于 `agent-detail.html:193-203`)；Skills chip rail (`agent-detail.html:204-209`)；当前任务（run id + 进度条 + elapsed + 百分比 `agent-detail.html:210-215`)。右 overview `.overview` (`agent-detail.html:127-138`) `role="tablist"` (`agent-detail.html:128`) **4 tabs**：**Activity**(`agent-detail.html:129` 默认选中) / **Instructions**(`agent-detail.html:130`) / **Skills**(`agent-detail.html:131`) / **Logs**(`agent-detail.html:132`)。切 tab (`switchTab` `agent-detail.html:338-344`) 更新 `aria-selected`/`tabindex` 重渲染 `tab-body`；键盘 ArrowRight/Left/Home/End (`agent-detail.html:327-337`)。≤1024px grid 折叠单列、inspector 不再 sticky (`agent-detail.html:84`) | console baseline 是单列 drawer。**M4.1** 落地为独立路由 `apps/console/src/app/agents/[id]/page.tsx` → `components/agent-detail-view.tsx`：320px 左 inspector + 右 `role="tablist"` 4 tabs（Activity/Instructions/Skills/Logs，roving tabindex + Arrow 键），复刻 design grid + ≤1024px 折叠 |
| 3.2 | activity 30 桶 sparkline | [✓ M4.1 @ac2927f] | `agent-detail.html:219-252` `renderActivity`；数据 `a.activity`（30 个 `{total,ok,fail}` 桶，`agents-data.js` `buckets()` helper `agents-data.js:8-16` 前补零到整 30 天 oldest→newest）。渲染：3-up KPI 行（30 天总运行/成功率/失败次数 `agent-detail.html:237-241`)；「运行趋势（30 天）」标签 (L242)；SVG 条形图 `.act-chart` viewBox `0 0 600 120` (`agent-detail.html:243`)，每桶一根 `<rect class="act-chart-bar">` 高 ∝ total/max，顶上叠红色 `<rect class="act-chart-bar fail">` 表失败 (`agent-detail.html:227-235`)；轴「30 天前 / 今天」(`agent-detail.html:246`)；最近活动 log 列表 (`agent-detail.html:247-250`) | baseline drawer 里的 sparkline 语义不同。**M4.1** 落地为 `components/agent-activity-sparkline.tsx`：30 桶 `{total,ok,fail}` SVG 条形图 + 失败叠层 + 3 KPI 行 + 「30 天前/今天」轴，从 `agents.activity` JSONB 渲染 |

### 3.2 console status

> **[✓ M4.1 @ac2927f] / [✓ M4.2 @426d703]** agent-detail 已落地为独立路由 `apps/console/src/app/agents/[id]/page.tsx` → `components/agent-detail-view.tsx`（两栏 split + 4 tabs + 30 桶 activity sparkline）。M4.2 接入 WS live-status（`agent-updated` 帧 → availability/status 刷新，WS 断线时轮询 fallback；见 [§后端契约 1](#后端契约-1-get-agentsid) 上方 M9.1 注与 `contracts/ws.ts`）。下方 baseline 描述为审计建稿时（HEAD `710b863`）的 MISSING 状态，保留以示对照。

**MISSING as page/route**；部分 drawer 存在但缺两栏 split、tabs、30 桶 activity 图。

- `apps/console/src/app/agents/` 仅 `page.tsx`（列表渲染 `<AgentsView />`）；`find apps/console/src/app/agents -type d` 只有目录本身，**无 `[id]/`、无 `app/agents/[id]/page.tsx`**。无 agent-detail 路由。
- console 把 agent detail 实现为**滑出 drawer** `AgentDrawer`（`apps/console/src/components/agents-view.tsx:410-510`），经列表行点击打开 (`openId` state `agents-view.tsx:95`)，无 URL、无返回键、无 `?id=` 路由。
- drawer body `DrawerBody` (`agents-view.tsx:518-636`) 是**单一滚动列**——无 inspector/overview 两栏 grid、无 tablist、无 4 tabs。渲染 4 个堆叠段：元数据(kv dl `agents-view.tsx:523-547`) / 能力描述符(card-flat 含 input/output schema + summary `agents-view.tsx:549-572`) / 当前任务(run + bar + elapsed `agents-view.tsx:574-596`) / 资源占用(`agents-view.tsx:598-610` 含 SVG `.mini-spark` viewBox `0 0 240 36`) / 最近日志(`agents-view.tsx:612-630`)。
- 那个 sparkline **语义与 design 不同**：`buildSparklinePath` (`agents-view.tsx:634-654`) 画的是**近期任务耗时(ms) 折线图**，非 design 的 30 天每日 total/fail 条形图。无 30 桶数据、无失败叠层、无 30 天总运行/成功率/失败次数 KPI 行、无「30 天前/今天」轴。
- 无 **Instructions** 面板（design `renderInstructions` `agent-detail.html:254-264` 显系统提示 + schema）；无 **Skills** 网格（design `renderSkills` `agent-detail.html:266-289`）。drawer 里 skills 根本不渲染，instructions/系统提示也不渲染。
- API 路由**存在**：`apps/console/src/app/api/agents/[id]/route.ts`（代理 GET 到 gateway `${gatewayUrl()}/api/v1/dispatch/agents/:id`）+ `apps/console/src/app/api/agents/[id]/logs/route.ts`。它们喂 drawer（`fetchAgentDetail`/`fetchAgentLogs` 在 `apps/console/src/lib/agents-catalog.ts`），不是喂 detail 页。

**→ 对应里程碑**: M4（agent-detail）

---

## 4. agents

**design**: `design/agents.html` + `design/js/app.js` + `design/js/agents-data.js`
**console**: `apps/console/src/app/agents/page.tsx` → `apps/console/src/components/agents-view.tsx` (+ `lib/agents.ts` / `lib/agents-catalog.ts` / `app/api/agents/route.ts` / `app/api/agents/[id]/route.ts`)

| # | Delta 项 | 状态 | design 证据 (file:line) | console 证据 (file:line) | 说明 |
|---|---|---|---|---|---|
| 4.1 | scope tabs (mine / all / archived) | [✓ M5.1 @8fe1164] | `agents.html:169-173` 三档 + `.cnt`；`agents.html:287-297` `updateScopeCounts`；`visibleRows` 应用 `agents.html:449-463` | baseline `agents-view.tsx` 无 `scope`/`mine`/`archived`。**M5.1** 落地：`Scope` 三档（我的/全部/已归档）+ `.cnt`；`archived` 收 failed/paused 行（`isArchived`），`mine` 计数恒 0（dispatch 代理无 owner 列，tab 仍在，对齐 flows-view） | console 无归属/归档隔离；失败/暂停 agent 未进独立 tab |
| 4.2 | filter chips (status / kind / role) | [✓ M5.1 @8fe1164]（简化单选，见说明） | design 用**多选下拉菜单** + 每值计数 + 选中徽章：`agents.html:183-205` `.menu-btn` 状态/类型/角色 + `.menu-pop`；`agents.html:300-317` `buildMenu`（带计数）；多选 toggle `agents.html:346-364`；徽章计数 `agents.html:378-388` | **M5.1** 落地为**单选 toggle chip**：`KIND_FILTERS`/`STATUS_FILTERS`/`ROLE_FILTERS` 固定集合，`aria-pressed` toggle，`toggleFilter` 单值或 null。**与 design 偏差（有意保留）**：design = 复选下拉（可同时多状态/多类型/多角色）+ 实时每值计数；console = 每组同时仅一 chip 激活、无计数；role 硬编码 4 值（`reader/coding/verify/orchestrator`）vs design 从数据动态计数。多选下拉 + 每值计数为 post-MVP 增强 |
| 4.3 | 排序 (sort controls) | [✓ M5.1 @8fe1164] | `agents.html:207-213` `btn-sort` 下拉 + `sort-label`；菜单项 `agents.html:319-335`（lastActive/name/runs/created + asc/desc radio）；sort 函数 `agents.html:434-445`；**可排序列头** `agents.html:228-230`(Agent,`data-sort="name"`) / `agents.html:234-236`(次数,`data-sort="runs"`)，活动指示 `agents.html:472-477` | baseline `agents-view.tsx` 无排序。**M5.1** 落地：`SortableHead` 可排序列头（`data-sort`/`data-active`/`data-dir` + `aria-sort`），`SortField = 'name' | 'load'`，`compareAgents` × dir。**与 design 偏差**：design 排序键含 `lastActive`/`runs`/`created`；console 实际可排键是 `name`/`load`（catalogue 无 last-active 时间戳且无运行次数列，`load` 列对齐 design「负载」）。默认 `load`/`desc`。方向可感知修复见 `3a9d39a`（aria-sort + CSS 箭头旋转） | console 行只能按 gateway 返回顺序，不能按 name/运行次数/最近活跃/创建时间重排，也不能翻转升降序 |
| 4.4 | result-count | [✓ M5.1 @8fe1164]（简化，见说明） | `agents.html:221` `#result-count`；`agents.html:470` 填 `${rows.length} / ${total}`（`total` 是当前 scope 全量，每 scope 重算 `agents.html:467-469`） | **M5.1** 落地 `.result-count` 显示 `可见 / 全量`。**与 design 偏差（有意保留）**：design 的 `total` 是 per-scope 全量（每 scope 重算）；console 因 scope tabs 的 `mine`/`archived` 是客户端派生，分母为全量已取列表，语义窄于 design 的 per-scope 计数。per-scope 计数为 post-MVP 增强 | console 显示 `可见 / 全量 fleet`，非 `可见 / scopeTotal`；因无 scope tabs，分母恒为全量已取列表，语义窄于 design 的 per-scope 计数 |

### 4.x other gaps (agents)

> **[✓ M5.1 @8fe1164]** 列表页主交互（scope tabs / filter chips / 排序 / result-count）已落地。下列 4.x 次要 gap 多为**有意推迟**（post-MVP），逐条标注；KPI 行 / 列表·看板 toggle / detail drawer 是 console 额外（见文末 ➕）。

- **批量多选 + 批操作栏**: ⬜ post-MVP — design `agents.html:227` 全选 checkbox + `agents.html:505` 行 checkbox + 固定底部 `.batch-bar` (`agents.html:247-253`，查看详情/复制/归档 `agents.html:590-601`)；console 无行 checkbox、无全选、无批栏、无批操作。
- **行 ⋯ popover**: ⬜ post-MVP — design `agents.html:519-525` `.row-more` + `.row-pop`（查看详情/复制配置/归档）；console 行 (`agents-view.tsx:274-337`) 点开 drawer 但无每行 ⋯ 菜单、无复制配置、无内联归档。
- **30 天 activity sparkline 列**: ⬜ post-MVP — design `agents.html:233` 表头「运行(30d)」+ `agents.html:515` `sparkSVG(a.activity,…)`，SVG 生成器 `agents.html:419-432`（30 桶，fail/total 条）；console 列表无每行 sparkline，唯一 sparkline 在 drawer (`agents-view.tsx:524, 595-608`) 且画的是近期任务耗时。
- **运行次数 + 成本列**: ⬜ post-MVP — design `agents.html:234-237`（次数可排，成本）`agents.html:516-517`（`a.runCount`,`a.cost`）；console 列 (`agents-view.tsx:260-271`) 是 Agent/类型/角色/状态/当前 run/区域/负载/今日成本——无运行次数列。
- **列头排序指示**: [✓ M5.1] — design `agents.html:57-61` `.h.sortable` + `sort-ic` + `data-active`；console `SortableHead` 已带 `data-sort`/`data-active`/`data-dir` + `aria-sort`（`3a9d39a` 方向可感知）。
- **搜索清空(×) + 清空筛选按钮**: ⬜ post-MVP — design `agents.html:180` `.clear` ×；`agents.html:215-218` `.clear-filters`；逻辑 `agents.html:399-407`；console 搜索框 (`agents-view.tsx:227-234`) 无清空键、无清空筛选动作。
- **骨架屏行**: ⬜ post-MVP — design `agents.html:606-621` `showSkeleton`（6 行 shimmer `.sk-row`），CSS `agents.html:120-122`；console 只显示「加载中…」文本 (`agents-view.tsx:250-253`)。
- **toast 通知**: ⬜ post-MVP — design `agents.html:624-634` `toast()`（归档/复制确认）；console 无 toast 系统。
- **空态**: ⬜ post-MVP — design `agents.html:490-498` 图标 +「没有匹配的 agent」/「注册第一个 agent」+ 注册按钮；console 纯 muted「暂无 agent」(`agents-view.tsx:254-257`)。
- **「+ 注册 Agent」页操作**: ⬜ post-MVP — design `agents.html:164` `<button id="new-agent">`，handler `agents.html:603`；console `PageShell` actions (`agents-view.tsx:133-155`) 仅 列表/看板 toggle + 刷新。
- **sticky scope-tab 计数 + 筛选徽章**: ⬜ post-MVP — design `agents.html:293-295` mine/all/archived 活计数 + `agents.html:378-388` 每菜单选中徽章；console scope tab 计数已落地（M5.1）但无筛选徽章。

> ➕ console 额外（非保真缺口，背景）：4 卡 KPI 行 (`agents-view.tsx:158-181`)、列表/看板 view toggle (`agents-view.tsx:135-150, 344-403`)、detail drawer (`agents-view.tsx:417-511`) —— design 无。

**→ 对应里程碑**: M5（agents）

---

## 5. index

**design**: `design/index.html` + `design/js/app.js`
**console**: `apps/console/src/app/page.tsx` → `<ChatView />`（+ `components/app-shell.tsx` / `page-shell.tsx` / `root-gate.tsx` 提供 chrome）

design 的 `index.html` 是 launcher/overview 页（hero + module grid + arch-strip）。console 根路由 (`apps/console/src/app/page.tsx:12`) 渲染 `<ChatView />`，**无任何 landing/overview 面**。shell 只提供 chrome（sidebar/topbar/main frame），不含 index body。

| # | Delta 项 | 状态 | design 证据 (file:line) | console 证据 (file:line) | 说明 |
|---|---|---|---|---|---|
| 5.1 | hero CTA | [✓ M6.1 @168b22e] | `design/index.html:108-132` `.hero`；CTA 按钮 `design/index.html:113-116`（「进入资源看板」/「查看 AgentFlows」）；hero eyebrow+title+sub `design/index.html:110-112`；4 hero stats（注册 agents/今日 runs/今日成本/在线 Daemon）`design/index.html:118-131` | baseline `apps/console/src/app/page.tsx` 返回 `<ChatView />`。**M6.1** 落地：根路由改 `components/launcher-view.tsx`（hero eyebrow/title/sub + 2 CTA Link → `/dashboard` + `/flows` + 4 hero stats），原 `<ChatView />` 迁至 `/chat` 路由，sidebar 编排区指向 `/chat`，品牌区改为返回概览链接，根面包屑改「概览」 | spec §1.10 故意把 chat 放根路由；M6.1 推翻为 launcher（hero CTA + 4 KPI stats 落地） |
| 5.2 | arch-strip | [✓ M6.1 @168b22e]（4→7 层扩展，见说明） | `design/index.html:175-180` `.arch-strip` 4 步（01 接入/02 网关/03 编排/04 适配）；CSS `design/index.html:63-73` | baseline console 全源码无 `.arch-strip` class。**M6.1** 落地 `.arch-strip` 7 层（接入/网关/编排/适配/调度/版本/存储+观测），`lib/launcher.ts` `ARCH_STEPS`。**与 design 偏差（有意扩展）**：design 仅 4 步（01 接入/02 网关/03 编排/04 适配）；console 按 `architecture-v0.2 §3.1` 补全 7 层（调度/版本/存储+观测），对齐平台实际分层 | 4 步平台分层图（轻量 Chat+Flowise 画布 → 鉴权限流 → Agentflow V2 状态机 → 中央 dispatch+本地 Daemon）无对应 |

### 5.x other gaps (index)

> **[✓ M6.1 @168b22e]** launcher（hero CTA + arch-strip）已落地。module-grid 仍缺；shell chrome 项已一致。

- **module grid**: ⬜ post-MVP — design `design/index.html:135-172` 6 卡（资源看板/Agents/AgentFlows/Lab/Workspace/设置，含图标/desc/foot meta/hover 上浮）；console 无 module grid，导航仅在 sidebar (`app-shell.tsx:120-138`, `nav.ts:25-41`)。
- **section labels**（「模块」`design/index.html:134`、「平台分层」`design/index.html:174`）：⬜ post-MVP（随 module grid 一起落地）。
- **品牌副标「控制台 · v0.2」**: 一致复现（`app-shell.tsx:117`），shell chrome 本身忠实——缺口只在 page body。
- **topbar 搜索 placeholder**: 一致（「搜索 agents / flows / runs…」 `app-shell.tsx:170` vs `design/index.html:97`）。
- **⌘K 绑定**: 一致（`app-shell.tsx:66-76` vs `design/js/app.js:222-229`）。
- **sidebar 折叠**: 一致（`app-shell.tsx:47-53` vs `design/js/app.js:131-145`），console 持久化到 `od:sidebar` vs design 仅 `data-collapsed` 属性。

**→ 对应里程碑**: M6（index）

---

## 6. workspace

**design**: `design/workspace.html` + `design/js/app.js` + `design/js/workspace-data.js`
**console**: `apps/console/src/app/workspace/page.tsx` → `apps/console/src/components/workspace-view.tsx` (+ `chat-view.tsx` / `lib/workspaces.ts` / `styles/workspace.css` / `app/api/workspaces/**` / `app/api/chat/route.ts`)

console Workspace view 是三栏布局的忠实移植，但项目列表在左列而非 sidebar（design 把项目移到 sidebar，`design/workspace.html:98` 注「left project list removed: projects now live in the sidebar」）。filter chips 渲染了但 4 枚里 3 枚是 disabled no-op。

| # | Delta 项 | 状态 | design 证据 (file:line) | console 证据 (file:line) | 说明 |
|---|---|---|---|---|---|
| 6.1 | ws-chat filter chips — 全部 | [✓ M7.1 @cd520b9] | `design/workspace.html:106` `全部` `aria-pressed="true"`；toggle `design/workspace.html:263-266` | `apps/console/src/components/workspace-view.tsx:405-407`（启用，`aria-pressed="true"`） | 唯一功能 chip；console 连 toggle 都没挂（无 onClick） |
| 6.1 | ws-chat filter chips — @我 | [✓ M7.1 @cd520b9]（真过滤，见说明） | `design/workspace.html:107` `@我` 可 toggle | **M7.1** 落地：`@我` 启用并真过滤（保留 owner 作者的回合）。评审修复 `a0d0360` 把三枚 chip 从 disabled 改为真过滤 | 渲染但 disabled no-op |
| 6.1 | ws-chat filter chips — 未读 | [✓ M7.1 @cd520b9]（真过滤） | `design/workspace.html:108` `未读` 可 toggle | **M7.1** 落地：`未读` 启用并真过滤（保留无 bot 回答的回合——run 仍在跑）。评审修复 `a0d0360` | 同上 |
| 6.1 | ws-chat filter chips — 含 run | [✓ M7.1 @cd520b9]（真过滤，见说明） | `design/workspace.html:109` `含 run` 可 toggle | **M7.1** 落地：`含 run` 启用并真过滤（保留带 runId 的回合）。**与 design 偏差**：design chip toggle 仅视觉无真过滤；console 让它们真过滤（保真增强） | 同上；design 的 chip toggle 也仅视觉无真过滤，但 console 让它们可见 disabled |
| 6.2 | ws-meta — members 段 | [✓ M7.1 @cd520b9]（简化，见说明） | `design/workspace.html:126-131` 3 命名成员（饶哲 owner/林敏 editor/邓凯 editor）+ 第 4 「+21 其余成员」折叠行 | **M7.1** 渲染 `detail.members`。**与 design 偏差（有意保留）**：console 全量平铺成员，丢「+N 其余成员」折叠 affordance；avatar 单一 muted 样式 vs design 每成员着色 avatar。折叠行 + 着色 avatar 为 post-MVP | console 全量平铺成员，丢「+21 其余成员」affordance |
| 6.2 | ws-meta — linked flow card | [✓ M7.1 @cd520b9] | `design/workspace.html:132-139` `flow_repro_01` + running status + 「论文批量复现流水线 · v2.3.1」note + 「在 AgentFlows 打开 →」链接 | `apps/console/src/components/workspace-view.tsx:546-571` 渲染 `detail.flows` 含 name/status/note + Link 到 `/flows`。评审修复 `a0d0360` 接入真实 flow status | 忠实；console 用 `/flows` href (`workspace-view.tsx:565`) vs design `agentflows.html`——同目标 |
| 6.2 | ws-meta — quota (本月) | [✓ M7.1 @cd520b9] | `design/workspace.html:140-145` 3 行（成本 $1,820/$5,000 36%；runs 412/2,000 21%；tokens 18.4M/80M 23%）带 `.bar` 填充 | `apps/console/src/components/workspace-view.tsx:573-578` + `QuotaRow` 610-622；`quotaBars` (`workspaces.ts:220-252`) 派生 3 条带 warn/danger 色调 | console 加 80%/100% warn/danger 色调 (`workspaces.ts:180-184`)——保真增强；值来自 `workspace.quota` jsonb，未设时显示 `— / —` |
| 6.2 | ws-meta — artifacts (产物) | [✓ M7.1 @cd520b9] | `design/workspace.html:146-153` 报告 3/数据集 7/代码 patch 12 | `apps/console/src/components/workspace-view.tsx:580-596` 同三行 from `detail.artifacts` | 忠实 |
| 6.2 | ws-meta — 额外「状态」段 | ➕ | （design 无） | `apps/console/src/components/workspace-view.tsx:598-604` 额外「状态」段显示项目状态 chip | console 增段，无害 |
| 6.2 | ws-meta — header title | [✓ M7.1 @cd520b9] | `design/workspace.html:123`「项目信息」/动态 `meta-title` | `apps/console/src/components/workspace-view.tsx:468-470` `selected?.name ?? '项目信息'` | 一致 |

### 6.x other gaps (workspace)

> **[✓ M7.1 @cd520b9]** filter chips + ws-meta 关联 flow 卡已落地（评审修复 `a0d0360`：含 run 真过滤 / flow 真实 status / @我 提示 / 清理）。下列 6.x 为**有意推迟**（post-MVP）。

- **项目列表位置**: ⬜ post-MVP（结构性偏离） — design 把项目放 sidebar（`design/js/app.js:79-108`，可展开 `.nav-project` 含任务预览）；console 放左 `.ws-list` 列 (`workspace-view.tsx:315-389`)，sidebar 无项目列表（`nav.ts:39` 是一条扁平 Workspace 链接）。这是对 v0.2 design handoff 的结构性偏离。
- **thread / message 布局**: [✓ M7.1] — design thread（`design/workspace.html:208-228` `renderThread`）用 `.cmsg` 气泡（avatar + name + time + 可选 `· R-8821` run tag + 附件 chip + 日分隔）；console 移植 (`workspace-view.tsx:487-514` `MessageBubble`，CSS `workspace.css:47-62`) class-for-class 一致。但 bot 回合全标「orchestrator」(`workspaces.ts:419`) vs design 每 agent 名（orchestrator-01/verifier-07/reader-04/coder-12，`design/js/workspace-data.js`）；console 无法区分哪个 agent 回答，因为 thread 由 `runs` 行派生而非 per-agent 消息。
- **composer**: [✓ M7.1] — design (`design/workspace.html:113-118`) textarea + 发送按钮，Enter 发送 / Shift+Enter 换行 (`design/workspace.html:261`)；console (`workspace-view.tsx:434-463`) 一致，并加无 linked flow 时 disabled 态 + 内联 sendError。design placeholder「在项目频道发消息，@agent 可派发任务…」vs console「在项目频道发消息，Enter 发送，Shift+Enter 换行…」——console 丢了「@agent 可派发任务」提示（从 UI dispatch 是 post-MVP，`workspace-view.tsx:34-37`）。
- **inline run cards**: [✓ M7.1] — design 把 run id 内联在消息头（`· R-8821`，`design/workspace.html:220`）；console 同样 (`workspace-view.tsx:498`，`.cmsg-run` CSS `workspace.css:62`)。但 design thread 展示一项目内多 agent 来回（`workspace-data.js:12-24` 5 条来自 3 agent）；console 的 `threadToMessages` (`workspaces.ts:383-430`) 把每个 run 拆成恰好一 user + 一 bot 消息，单 run 永不产生多回合 agent 对话——thread 读作 Q&A 对而非多 agent chat。
- **page actions**: ⬜ post-MVP — 「归档」+「+ 新建项目」console 均 disabled (`workspace-view.tsx:304-309`) vs design 启用（仅视觉）(`design/workspace.html:92-94`)。
- **chat-head sub**: ⬜ post-MVP — design「24 成员 · 关联 flow flow_repro_01 · 今日 18 条消息」(`design/workspace.html:104`)；console 显示成员数 + flow 名但无「今日 N 条消息」(`workspace-view.tsx:395-403`)。
- **new-task 物化**: ⬜ post-MVP — design 的 `?new=1` 流程 (`design/workspace.html:176-201`) 从 new-task 页创建新项目线程；console `/tasks/new` 的 `doSend` 已 `router.push('/workspace?new=1&task=&dir=&flows=&agents=&contextRefs=…')` 构造交接 query（`new-task-view.tsx:365-383`），但 `/workspace` 页**尚不消费** `?new=1` query（无 `useSearchParams` 读取，M9 契约接线后续）。

**→ 对应里程碑**: M7（workspace/lab）

---

## 7. lab

**design**: `design/lab.html` + `design/js/app.js` + `design/js/lab-data.js`
**console**: `apps/console/src/app/lab/page.tsx` → `apps/console/src/components/lab-view.tsx` (+ `lib/lab.ts` / `lib/lab-proxy.ts` / `styles/lab.css` / `app/api/lab/sessions/**`)

console Lab view 是**最忠实的移植**——sessions、线程化 messages、@mention、thinking、tool-card 都渲染。主要偏离在右侧 artifacts 面板：design 有结构化 假设/数据/代码/可复现快照 分组；console 把 thread 汇成 agent-set/tool-calls/run-ids（无 `lab_artifacts` 表，`lab.ts:39-45`）。

| # | Delta 项 | 状态 | design 证据 (file:line) | console 证据 (file:line) | 说明 |
|---|---|---|---|---|---|
| 7.1 | sessions 列表/选择器 | ✅ | `design/lab.html:121-124` `.sessions`（head「实验会话 · 12」+「+」新建按钮）；JS 渲染 `design/lab.html:204-214` 每项 `.nm` name + `.ds` 2 行 desc + 状态 chip（进行/暂停/完成）+「N agents」mono meta；点击切 chat title/sub (`design/lab.html:279-285`) | `apps/console/src/components/lab-view.tsx:300-346` 渲染 `.session-item` 含 name/desc/status chip/agentsCount；`sessionStatusLabel` (`lab.ts:152-161`) 镜像 进行/暂停/完成；点击设 `selectedId` 触发 detail fetch (`lab-view.tsx:112-138`) | 忠实列表。两缺口：(1) design head 有「+ 新建」icon 按钮 (`design/lab.html:122`)——console head (`lab-view.tsx:301-303`) 省略；(2) design session 切换仅视觉（只换 title/sub `design/lab.html:282-284`）；console 真正 fetch + 重渲染整 thread——保真增强 |
| 7.1 | sessions 数据源 | ✅ | `design/js/lab-data.js:3-9` 5 硬编码会话（RL 复现/多模态对齐/营销 A/B/TF→PyTorch/跨域综述） | `apps/console/src/lib/lab.ts:412-418` `fetchLabSessions` → `/api/lab/sessions` → gateway；`LabSessionSummary` 类型 `lab.ts:46-59` | console 接真实数据；design 是静态 fixture |
| 7.2 | 线程化 messages（多 agent 对话） | ✅（扁平） | `design/lab.html:217-234` `renderMessages`：扁平时序 `.msg` 流（avatar + name + roleTag + time + 气泡 + 可选 thinking + 可选 tool-card），**无缩进/回复嵌套**（`lab-data.js` 消息无 parentId；「线程化」指多 agent 线程而非缩进回复） | `apps/console/src/components/lab-view.tsx:382-394` + `MessageBubble` 480-501 同扁平流同字段；`messagesToThread` (`lab.ts:360-387`) 保留 `parentId` 但扁平渲染 (`lab.ts:25-27, 354-357` 文档 parentId 为未来缩进视图) | 忠实。console 加日分隔 (`lab-view.tsx:484`，`dayLabel` `lab.ts:333-342`)；design lab 无日字段所以 design 实际不显日分隔，console 会算。console 还在头加 `· runId` (`lab-view.tsx:492`)，design 省略 |
| 7.2 | 线程化 — 多 agent 角色 | ✅ | `design/lab.html:39-42` CSS 5 avatar 变体（human/orchestrator/reader/coder/verifier）各着色；`design/js/lab-data.js:12-35` 5 角色会话 | `apps/console/src/lib/lab.ts:164-188` `roleAvatarClass`/`roleInitial`/`roleName`/`roleTag` 同 5 角色；`lab.css:51-55` 逐字移植 5 avatar 色 | 忠实角色调色板。console `roleName` 回退 agent_id (`lab.ts:191-207`)，真实 agent id 显示而非硬编码「orchestrator-01」 |
| 7.3 | @mention — composer chips | ✅ | `design/lab.html:148-151` 4 `.mention` chips（@orchestrator/@reader/@coder/@verifier）；点击插入 textarea (`design/lab.html:240-244`) | `apps/console/src/components/lab-view.tsx:416-427` 映射 `LAB_MENTION_HANDLES` (`lab.ts:147`) 为可点 chip；`insertMention` (`lab-view.tsx:213-221`) 追加 `@handle ` 到 composer | 忠实。console chips 是 `<button>`（a11y）vs design `<span>`——增强 |
| 7.3 | @mention — body 内联着色 | ➕（console 有，design 缺） | design **不**着色内联 mention——`design/lab.html:227` 把 `${m.body}` 作 raw HTML 渲染；`lab-data.js` body 含 `@reader`/`@coder`/`@verifier` 纯文本 | `apps/console/src/components/lab-view.tsx:467-477` `renderBody` + `lab.ts:281-290` `splitBodyMentions` 把 `@handle` 着色为 `.msg-mention`；CSS `lab.css:70-71` | console 优于 design：内联 @mention 是着色 chip。`MENTION_RE` (`lab.ts:240`) 正确排除 email-ish `user@host` |
| 7.4 | thinking（推理块） | ✅ | `design/lab.html:60` CSS `.thinking`（斜体 muted 左边框）；JS `design/lab.html:228` 渲染 `💭 ${m.thinking}`；样本 `lab-data.js:16,25,33` | `apps/console/src/components/lab-view.tsx:495` `<div className="thinking">💭 {message.thinking}</div>`；`lab.css:78` 移植样式；`LabMessage.thinking` (`lab.ts:86`) 带字段 | 忠实，含 💭 前缀 + 左边框斜体样式 |
| 7.5 | tool-card（工具调用卡） | ✅ | `design/lab.html:55-58` CSS `.tool-card`（mono，`.th` 名含 🛠 / `.td` input / `.tr` output）；JS `design/lab.html:229` 渲染 `🛠 ${m.tool.name}` + input + output；样本 `lab-data.js:19,22,30`（read_paper/run_sandbox/eval_compare） | `apps/console/src/components/lab-view.tsx:496` + `ToolCard` 504-512 同 `.tool-card`/`.th`/`.td`/`.tr` 结构含 🛠 前缀；`lab.css:73-76` 移植 CSS；`normalizeToolCall` (`lab.ts:303-330`) 映射 jsonb → `{name,input,output}` | 忠实。console `normalizeToolCall` 额外容忍 Flowise `usedTools` 数组形状 (`lab.ts:316-327`)——健壮性增强 |
| 7.5 | tool-card — artifacts 面板工具列表 | [✓ M7.x @64686ff]（简化，见说明） | `design/lab.html:172-176` 代码组列 `patch_7a3f9c.diff`（+86 −24 · 已提交）+ `eval_notebook.ipynb`（12 cells · 可复现） | **M7 落地** `apps/console/src/components/lab-view.tsx:563-577`「工具调用」组列 thread 里 tool 名（`t.name`）+ 截断 input。**与 design 偏差（有意保留）**：console 复用 tool-call 名，非结构化代码 artifacts（无 `lab_artifacts` 表，`lab-view.tsx:39-45` 文档为有意 post-MVP） | console 复用 tool-call 名，非结构化代码 artifacts（无 `lab_artifacts` 表） |

### 7.x other gaps (lab)

> **[✓ M7.x @64686ff]** lab sessions + 线程化 messages + @mention + thinking/tool-card 已落地。下列 7.x 多为**有意推迟**（post-MVP）。

- **artifacts 面板 — 假设 (Hypotheses)**: ⬜ post-MVP — design 有结构化 假设 组，H1/H2/H3 卡含 置信度 + 验证状态 (running/queued/failed) (`design/lab.html:161-166`)；console 全无假设组——`ArtifactsPanel` (`lab-view.tsx:522-596`) 渲染 协作 agents/工具调用/可复现快照。`lab-view.tsx:39-45` 文档为有意（post-MVP）。
- **artifacts 面板 — 数据 (Data files)**: ⬜ post-MVP — design 列 `results_skip.csv`/`results_baseline.csv` 含行数 + 大小 (`design/lab.html:167-171`)；console 无数据文件组。
- **artifacts 面板 — 可复现快照**: [✓ M7.x]（简化） — design `⬢ run R-8821 · v2.3.1` 含 `sha 7a3f9c · input 锁定` (`design/lab.html:177-180`)；console `⬢ run {r.slice(0,8)}` 含 `run_id · OTel 全链路` (`lab-view.tsx:586-590`)——同概念，少元数据（无 sha/version/input-locked 徽章）。
- **artifacts 面板 — header count chip**: [✓ M7.x]（语义偏差） — design `chip-outline`「4」(`design/lab.html:159`)；console 显示 `{thread.length}` (`lab-view.tsx:446`)——消息数非 artifact 数，语义不同。
- **chat-head sub**: ⬜ post-MVP — design「4 agents · 已运行 12m · 预算 $18 / 已用 $6.40」(`design/lab.html:131`)——runtime + budget；console「N agents · N 条消息 · 自动/介入模式」(`lab-view.tsx:353-357`)——无 runtime 无 budget（`LabSessionDetail` 无此字段 `lab.ts:92-105`）。
- **mode switch**: [✓ M7.x]（增强） — design 仅视觉——`design/lab.html:269-276` 只 toggle `mode-hint` 文本不持久；console 经 PATCH `/api/lab/sessions/:id` 持久化 (`lab-view.tsx:226-245`，`lab.ts:437-449`)——保真增强（真持久化）。
- **「+ 新实验」按钮**: ⬜ post-MVP — design 启用 (`design/lab.html:115`)；console disabled title「+ 新实验（MVP 后接入完整配置）」(`lab-view.tsx:307-308`)。
- **「归档会话」按钮**: [✓ M7.x]（增强） — design 启用仅视觉 (`design/lab.html:114`)；console 接真 PATCH status→done (`lab-view.tsx:251-274`)——增强。
- **agent 自主回环**: ⬜ post-MVP — design 在每次人类发送后 700ms 模拟一条 agent 回复 (`design/lab.html:257-264`)；console 只追加人类回合，无 agent 自动回复 (`lab-view.tsx:153-201`)。`lab-view.tsx:31-34` 文档说明 agent 自主回复是 dispatch/daemon 的事，超出前端任务。
- **sessions-head「+ 新建」icon 按钮**: ⬜ post-MVP — design 有 (`design/lab.html:122`，SVG plus，`id="new-session"`)；console `sessions-head` (`lab-view.tsx:316`) 完全省略。

**→ 对应里程碑**: M7（workspace/lab）

---

## 8. dashboard

**design**: `design/dashboard.html` + `design/js/app.js`（数据散见 `agents-data.js`/`flows-data.js`/`tokens-data.js`）
**console**: `apps/console/src/app/dashboard/page.tsx` → `apps/console/src/components/dashboard-view.tsx` (+ `lib/fleet-stats.ts` / `styles/dashboard.css` / `app/api/fleet-stats/route.ts`)

| # | Delta 项 | 状态 | design 证据 (file:line) | console 证据 (file:line) | 说明 |
|---|---|---|---|---|---|
| 8.1 | density（密度） | [✓ M8.1 @524aa2d]（语义替换，见说明） | `design/dashboard.html:160-178` 卡片；`:282-362` canvas（120×42 = 5040 格，6 态 idle/ready/running/queued/failed/paused，动画 ~3%/1.5s，`density-hover` tooltip `[c,r] ~206 实例`），图例 `:170-177` | **M8.1** 落地 `apps/console/src/components/dashboard-view.tsx:193-230`；`fleet-stats.ts:452-480` `deriveDensityView` 平铺 `fleet.tasks.byStatus`；`dashboard.css:80-105` `.density-grid` 7px 格，cap `DENSITY_MAX_TILES=5000` (`fleet-stats.ts:229`)。**与 design 偏差（有意替换）**：console 明确**不**渲染 1M-agent canvas 热图（百万实例密度需 per-agent 实时状态，单快照 API 不返回）；它把 live `dispatch_tasks` 队列按生命周期状态（queued/claimed/running/completed/failed `fleet-stats.ts:249-256`）平铺成状态着色马赛克。语义不同（任务队列 vs agent 密度），无动画，无 hover tooltip（仅每格 HTML `title` `dashboard-view.tsx:213`）。1M-agent canvas 待 timeseries-bucket API（post-MVP） | console 明确**不**渲染 1M-agent canvas 热图；它把 live `dispatch_tasks` 队列按生命周期状态（queued/claimed/running/completed/failed `fleet-stats.ts:249-256`）平铺成状态着色马赛克。语义不同（任务队列 vs agent 密度），无动画，无 hover tooltip（仅每格 HTML `title` `dashboard-view.tsx:213`） |
| 8.2 | donut | [✓ M8.1 @524aa2d]（维度不同，见说明） | `design/dashboard.html:180-191` 卡片；`:365-399` 6 段（运行中 62%/就绪 21%/空闲 9%/排队 5%/人工暂停 2%/失败 1%；中心 `1.04M 总实例`） | **M8.1** 落地 `apps/console/src/components/dashboard-view.tsx:232-262`；`fleet-stats.ts:542-552` `daemonSegments`；`:232-237` `DAEMON_STATUS_META`（online/offline/draining/unknown）。**与 design 偏差（有意替换）**：console donut 表 **daemon** 状态（4 态：在线/离线/排空中/未知）非 **agent** 状态（design 6 态 + `1.04M 总实例` 中心需百万级 agent 状态，平台 MVP 无此数据）。SVG donut 同惯例（`rotate(-90)`，stroke-dash 数学）`dashboard-view.tsx:450-472` | console donut 表 **daemon** 状态（4 态：在线/离线/排空中/未知）非 **agent** 状态。中心读 `daemons total` 非 `1.04M 总实例`。丢 2 个 design 态（就绪/人工暂停）。SVG donut 同惯例（`rotate(-90)`，stroke-dash 数学）`dashboard-view.tsx:450-472` |
| 8.3 | throughput | [✓ M8.1 @524aa2d]（KPI 网格代面积图，见说明） | `design/dashboard.html:195-213` 卡片；`:402-430` 24h 面积图 SVG（渐变填充、网格线、48 点、晨+午后峰；metric toggle `runs/min`/`P95 延迟`/`失败率`；时间轴 `00:00→now`） | **M8.1** 落地 `apps/console/src/components/dashboard-view.tsx:265-301`；`fleet-stats.ts:491-504` `deriveThroughputView`；`dashboard.css:107-130` `.throughput-grid` 2×2 KPI 网格。**与 design 偏差（有意替换）**：console 渲染 **2×2 数字 KPI 网格**（runs 完成/任务完成/runs 速率/min/run 成功率）而非面积图。无 SVG path、无渐变、无 metric toggle、无时间轴。注释 `fleet-stats.ts:155-159` 明确推迟面积图「直到有 timeseries-bucket API」 | console 渲染 **2×2 数字 KPI 网格**（runs 完成/任务完成/runs 速率/min/run 成功率）而非面积图。无 SVG path、无渐变、无 metric toggle、无时间轴。注释 `fleet-stats.ts:155-159` 明确推迟面积图「直到有 timeseries-bucket API」 |
| 8.4 | usage 表 | [✓ M8.1 @524aa2d]（轴不同，见说明） | `design/dashboard.html:244-250`「Provider 成本分摊」卡（4 行 Anthropic $2,174 52%/OpenAI $1,171 28%/本地 GPU $502 12%/其他 $335 8%；列 名称/bar/金额/占比） | **M8.1** 落地 `apps/console/src/components/dashboard-view.tsx:358-404`；`fleet-stats.ts:516-534` `usageRows`；`dashboard.css:63-66` `.usage-table`。**与 design 偏差（有意替换）**：console 渲染**按模型** token 用量表（列：模型/输入/输出/缓存读/调用/占比 含 `bar-mini`），非 design 的按 **Provider** 成本分摊（usage 数据来自 new-api/Langfuse 的 per-model token 计量，无 per-Provider 成本拆分）。design 的 Provider 卡（Anthropic/OpenAI/本地 GPU/其他）在 console 全无；console 的表 design 里根本没有 | console 渲染**按模型** token 用量表（列：模型/输入/输出/缓存读/调用/占比 含 `bar-mini`），非 design 的按 **Provider** 成本分摊。design 的 Provider 卡（Anthropic/OpenAI/本地 GPU/其他）在 console 全无；console 的表 design 里根本没有 |
| 8.5 | 时间窗 toggle | [✓ M8.1 @524aa2d]（3 窗代 4 窗，见说明） | `design/dashboard.html:120-125` 4 窗（`1h`/`24h`/`7d`/`30d`；`7d` 默认 `aria-pressed="true"`） | **M8.1** 落地 `apps/console/src/components/dashboard-view.tsx:124-135` 渲染 `FLEET_WINDOW_PRESETS`；`fleet-stats.ts:216-220` presets（`1h`/`24h`/`7d` 仅）；默认 `useState<number>(24)` `dashboard-view.tsx:64`。**与 design 偏差（有意）**：console 出 **3 窗**（1h/24h/7d），**缺 `30d`**（30d 窗需长留存 timeseries，MVP 未接），默认 **24h**（非 7d）。toggle 是 live 的（驱动 `fetchFleetStats`），非 design 那种仅视觉 | console 出 **3 窗**（1h/24h/7d），**缺 `30d`**，默认 **24h**（非 7d）。toggle 是 live 的（驱动 `fetchFleetStats`），非 design 那种仅视觉 |

### 8.x other gaps (dashboard)

> **[✓ M8.1 @524aa2d]** dashboard 五卡（density/donut/throughput/usage/时间窗）已落地，均按平台 MVP 实际数据源做了**有意语义替换**（见 8.1–8.5 说明）。下列 8.x 为**有意推迟**（post-MVP，依赖 timeseries-bucket / per-Provider 成本 API）。

- **KPI tiles 内容 + delta 偏离**: [✓ M8.1]（有意偏差） — design `design/dashboard.html:131-156` 出 4 KPI：活跃 agents（`▲3.2% · 24h`，sub `峰值 1.08M · 容量 1.25M`）、今日 runs（`▲12.1%`，sub `成功 96.4% · 失败 1.8% · 进行 2.1%`）、平均吞吐（`▲8.4%`，sub `P50 延迟 1.2s · P95 4.7s`）、今日成本（`▼4.6%`，sub `月预算 $135K · 已用 31%`）。console `dashboard-view.tsx:178-189` 出：注册 agents（sub `daemons 在线`）、{windowLabel} runs（delta = 成功率，无 ▲/▼ 趋势）、总 token（sub `调用`）、{windowLabel} 成本（sub `累计 · runs`）。**无 `▲/▼` 百分比趋势 delta**（需历史基线对比，MVP 无），无 P50/P95 延迟，无峰值/容量，无月预算已用 sub。KPI 集不同（总 token 取代平均吞吐）。
- **导出按钮缺**: ⬜ post-MVP — design `design/dashboard.html:126` `导出` 按钮 + `:433-435` CSV 导出 alert；console `dashboard-view.tsx:136-143` 仅 `刷新` 按钮，无导出。
- **事件流卡缺**: ⬜ post-MVP — design `design/dashboard.html:253-265` 事件流卡（8 事件，时间戳 `14:32`…`14:20`，标签 WARN/OK/INFO/ERR，live `实时` 状态，max-h 滚动）；console 全无。
- **Provider 成本分摊卡缺**: ⬜ post-MVP — design `design/dashboard.html:244-250`；console 成本 banner (`dashboard-view.tsx:331-355`) 只显 last24hCost + runsCounted + 一条「用量按模型分摊」meter bar，无 Anthropic/OpenAI/本地 GPU/其他 拆分（需 per-Provider 成本 API）。
- **Region % 列被替换**: [✓ M8.1]（有意偏差） — design `design/dashboard.html:218-225` region 行 = name/bar/`142K`/`78%`（agents + 百分比）；console `dashboard-view.tsx:316-324` = name/bar/`{agents} agents`/`{cost}`——把百分比列换成成本值；bar 宽是 `agents/maxRegionAgents` 非 design 绝对百分比。
- **sparklines / 面积图全无**: [✓ M8.1]（有意推迟） — design 出一张渐变面积图 `design/dashboard.html:402-430`（runs/min over 24h）；console 零 sparkline / 零时序视觉（待 timeseries-bucket API）。
- **刷新动画缺**: ⬜ post-MVP — design `design/dashboard.html:445-453` 点击时刷新图标转 360°（尊重 `prefers-reduced-motion`）；console 刷新按钮 (`dashboard-view.tsx:136-143`) 加载时只 toggle `disabled`。
- **density hover tooltip 缺**: [✓ M8.1]（有意简化） — design `design/dashboard.html:350-361` 显定位 `density-hover` div `[c,r] {status} · ~206 实例`；console 格 (`dashboard-view.tsx:208-214`) 仅 HTML `title` 属性（无定位 tooltip、无实例数估算）。
- **快照页脚（design 无）**: ➕ — console `dashboard-view.tsx:407-419` 出 `.dash-foot`（快照时间/窗口/sources ✓✗/本页刷新）；design 无对应（诚实覆盖度自白）。
- **部分数据 banner（design 无）**: ➕ — console `dashboard-view.tsx:157-162` + `fleet-stats.ts:560-565` `statusBadge` 在 Langfuse/new-api 未接或 usage 截断时显 `dash-banner`；design 无。

**→ 对应里程碑**: M8（dashboard/settings）

---

## 9. settings

**design**: `design/settings.html` + `design/js/app.js` + `design/js/tokens-data.js`
**console**: `apps/console/src/app/settings/page.tsx` → `apps/console/src/components/settings-view.tsx` (+ `lib/tokens.ts` / `lib/token-proxy.ts` / `lib/tokens-client.ts` / `styles/tokens.css` / `app/api/tokens/route.ts` / `app/api/tokens/[id]/route.ts`)

design 子导航 `design/settings.html:145-156` 定义恰好 6 tab（4 组）。console 子导航 `apps/console/src/components/settings-view.tsx:46-63` `TAB_GROUPS` 定义同 6 tab 同 4 组。

### 9.1 6-tab 枚举（design → console）

> **[✓ M8.2 @b43bfc8]** 6 tab 子导航 + 4 组结构已对齐 design。tab 1（API Key）是 live CRUD；tab 2–6 按 design 的 DOM 形状回填（model-row / toggle-row / kv / danger-zone 原语 + design 占位数据），数据接线延后但形状保真。

| # | design tab (组) | design 证据 | console 状态 | console 证据 |
|---|---|---|---|---|
| 1 | API Key 管理 (密钥) | `design/settings.html:147` `<a data-tab="keys" aria-current="true">`，section `:160-219` | [✓ M8.2] 有（live CRUD） | `settings-view.tsx:46-47`（组），`:158` `{tab === 'keys' && <ApiKeysTab />}`，`:172-496` `ApiKeysTab` |
| 2 | 默认模型 (模型) | `design/settings.html:149` `<a data-tab="models">`，section `:303-319` | [✓ M8.2] 形状对齐（占位数据） | `settings-view.tsx:48`（组），`:159` `<DefaultModelsTab />`，`:746-…` 按角色分派 model-row + 回退链 toggle-row（design 占位数据，待 Flowise 配置接口接入回填） |
| 3 | 预算与配额 (治理) | `design/settings.html:151` `<a data-tab="quota">`，section `:322-337` | [✓ M8.2] 形状对齐（占位数据） | `settings-view.tsx:50-53`（组），`:160` `<QuotaTab />`，全平台预算 bar + 熔断规则 toggle-row（design 占位数据） |
| 4 | 通知 (治理) | `design/settings.html:152` `<a data-tab="notify">`，section `:340-356` | [✓ M8.2] 形状对齐（占位数据） | `settings-view.tsx:50-53`（组），`:161` `<NotifyTab />`，通知事件 + 通知渠道 toggle-row（design 占位数据） |
| 5 | 账户与团队 (账户) | `design/settings.html:154` `<a data-tab="account">`，section `:359-377` | [✓ M8.2] 形状对齐（占位数据） | `settings-view.tsx:57-60`（组），`:162` `<AccountTab />`，个人 kv + 团队成员行 + 邀请按钮（design 占位数据） |
| 6 | 危险区 (账户) | `design/settings.html:155` `<a data-tab="danger">`，section `:380-387` | [✓ M8.2] 形状对齐（结构在，按钮 disabled） | `settings-view.tsx:57-60`（组），`:163` `<DangerTab />`，3 toggle-row（暂停/轮换/删除），按钮全 `disabled`（MVP 不触发真实破坏性操作） |

### 9.x other gaps (settings)

> **[✓ M8.2 @b43bfc8]** 6 tab 形状已对齐 design（model-row / toggle-row / kv / danger-zone 原语 + design 占位数据）。下列 9.x 为 API Key tab 的**实现差异**（部分有意，如 copy/reveal 因 new-api 已掩码不存原文）与其余 tab 的**数据接线推迟**（post-MVP）。

- **API Key tab — gateway 卡简化**: [✓ M8.2]（有意简化） — design `design/settings.html:166-183` gateway 卡：`gw-mark N`、`已连接` 状态、`https://new-api.internal:3000` URL、`v0.6.8`、`管理密钥 sk-newapi-••••••••3f`、3 统计（令牌 `8`/渠道 `6`/今日用量 `$3,118`）+ `编辑连接` 按钮。console `settings-view.tsx` `GatewayCard` 仅：`gw-mark N`、reachable 状态、`svc={health.svc}`、1 统计（令牌 count）。**无 URL、无 version、无 admin-key 掩码、无渠道数、无今日用量、无编辑连接按钮**（post-MVP 接 new-api 健康细节）。
- **API Key tab — 行操作图标换成文本字形**: [✓ M8.2]（有意简化） — design `design/settings.html:415-425, 488-504` 内联 SVG（copy/eye/eyeOff/edit/play/pause/del）；console `settings-view.tsx:429-459` 渲染文本字形（`∥`/`▶`/`✎`/`✕`）——无 SVG 图标集。
- **API Key tab — copy & reveal 动作丢**: [✓ M8.2]（有意，见说明） — design `design/settings.html:488-489` 每行 copy + reveal 按钮（`data-copy`/`data-reveal`，eye/eyeOff toggle）；console `settings-view.tsx:402-404` 仅渲染掩码 key `<span className="tk-key mono">`，**无 copy 按钮、无 reveal 按钮**（`tokens.ts:7-9` 注 new-api 已掩码，原始 key 永不到浏览器——安全增强，非缺口）。
- **API Key tab — token modal 字段不同**: [✓ M8.2]（有意适配） — design `design/settings.html:229-273` modal 字段：令牌名称/分组/额度(额度点)/过期时间/可用模型范围/备注/设为默认令牌(checkbox)/创建后启用(checkbox)。console `settings-view.tsx` `TokenModal`：令牌名称/分组/剩余额度(额度点)/过期时间/无限额度(checkbox)/备注。**无可用模型范围、无设为默认令牌、无创建后启用**；多了一个 design 没有的「无限额度」checkbox（适配 new-api remainQuota 存储模型）。
- **API Key tab — delete modal 缺 revoke checkbox**: [✓ M8.2]（有意简化） — design `design/settings.html:284-297` delete modal 有 `立即吊销所有已分发副本` checkbox（`del-revoke`，默认勾）+ 「是否需要同时吊销已分发的副本？」提示。console `settings-view.tsx` `DeleteModal` 完全省略 revoke checkbox（new-api 令牌删除即吊销，无独立副本概念）。
- **默认模型 tab — 全占位**: [✓ M8.2]（形状对齐，数据待接） — design `design/settings.html:303-318` 「按角色分派」卡含 5 `model-row`（阅读/编码/验证/编排/抓取，各 model + $/M in + 默认 chip）+ 「回退链」卡含 3 `toggle-row`（主/回退/兜底）。console `settings-view.tsx` 渲染 model-row + fallback-chain toggle-row（design 占位数据）；**待 Flowise 配置接口接入回填**。
- **预算与配额 tab — 全占位**: [✓ M8.2]（形状对齐，数据待接） — design `design/settings.html:322-336` 「全平台预算」卡（月预算 $135,000，`.bar` 31%，已用 $41,820，熔断阈值 90%）+「熔断规则」卡 4 `toggle-row`。console `settings-view.tsx` 两卡 + toggle-row（design 占位数据）；**待预算/熔断规则 API**。
- **通知 tab — 全占位**: [✓ M8.2]（形状对齐，数据待接） — design `design/settings.html:340-355` 6-toggle「通知」卡 + 3-toggle「通知渠道」卡。console `settings-view.tsx` 两卡 + toggle-row（design 占位数据）；**待通知配置 API**。
- **账户与团队 tab — 全占位**: [✓ M8.2]（形状对齐，数据待接） — design `design/settings.html:359-376` 「个人」卡（kv）+「团队 · 38 成员」卡含 3 成员 `model-row` + `邀请` 按钮。console `settings-view.tsx` 两卡 + 成员行 + 邀请按钮（design 占位数据）；**待 SSO/团队 API**。
- **危险区 tab — 结构在但 inert**: [✓ M8.2]（有意 inert） — design `design/settings.html:380-387` 3 toggle-row 含 live 按钮。console `settings-view.tsx` 渲染同 3 toggle-row 但**3 按钮全 `disabled`**（MVP 不触发真实破坏性操作：暂停全部运行/轮换全部令牌/删除 workspace）。文案还丢了第一行描述里的「645K」具体计数。
- **topbar 搜索框缺**: ⬜ post-MVP — design `design/settings.html:128` `搜索设置…` 输入 + `⌘K` kbd；console `PageShell` 不渲染 settings-scoped 搜索。
- **toast host**: [✓ M8.2]（简化） — design `design/settings.html:300, 514-521` 固定定位 `.toast-host` 含 `toast-in` 关键帧动画 + ok/err SVG 图标；console `settings-view.tsx:493` 渲染简单 `<div className="toast {kind}">{msg}</div>`，无关键帧动画无 SVG 图标（CSS `.toast-host` 未接）。
- **tab anchor vs button 语义**: [✓ M8.2]（有意 a11y 增强） — design `design/settings.html:147-155` 用 `<a data-tab="...">` 锚 + `aria-current`；console `settings-view.tsx:142-151` 用 `<button className="settings-tab">` + `aria-current`——语义从 anchor 改 button（轻微 a11y/SEO 偏移，button 更适合 SPA tab）。
- **quota 单位语义**: [✓ M8.2]（一致 + 适配） — design `design/js/tokens-data.js:4-6` 把 `used/total` 当「额度点」`1 点 ≈ $0.002` (`design/settings.html:250`)；console `settings-view.tsx:73` `POINTS_PER_DOLLAR = 500_000`（即 `1$ ≈ 500000 点` ≈ `1 点 ≈ $0.002`）与 design 单位一致。`ApiToken.remainQuota` 映射 new-api 的**剩余**预算（非 design 的「总额」授予），故 console bar 显 `usedQuota / totalQuota`，其中 `totalQuota = used + remain`（派生，`tokens.ts:163`）。对 bar 行为等价，但编辑表单回传 `remainQuota`（`tokens-client.ts:165-190`）非授予——对 new-api 实际存储模型的忠实适配。

**→ 对应里程碑**: M8（dashboard/settings）

---

## 后端契约 1: GET /agents/:id

> **[✓ M9.1 @530b3db]** gateway-owned `GET /api/v1/agents/:id` 已落地，把 platform-owned `agents` 表 1:1 映射到 design `agents-data.js` 单 agent 形状（`apps/gateway/src/routes/agents.ts` `toAgentDto`：id/name/kind/roles/instructions/skills/visibility/concurrency/model/runtime/owner/activity[{total,ok,fail}]/status/availability/summary + run-context 字段 run/flow/load/cost/progress/elapsed/inputSchema/outputSchema/created/lastActiveDays/runCount/failCount）。companion migration `1720000008001` 给 agents 表补 summary/input_schema/output_schema 列。验收 `agents-shape.test.ts`。
>
> ⚠️ **消费侧待迁移（M5 follow-up）**：console agents 列表 + agent-detail 仍走 dispatch 代理 `/api/v1/dispatch/agents/:id`（`apps/console/src/app/api/agents/[id]/route.ts`），未切到 gateway design-shape 路由；dispatch 路由返回 snake_case `agent_daemons` join（见下方 console shape）。下方字段级 delta 表记录**契约形状差异**（design vs dispatch 当前响应），作为迁移到 gateway design 路由时的对照——M9.1 已提供目标形状，迁移本身是后续 M5 任务。owner 经 workspace_members LEFT JOIN 解析为展示名；activity 强制 `{total,ok,fail}`（ok 缺失 = total−fail）；runCount/failCount 按 design L228-231 从 activity 盖印。

### design shape
源: `design/js/agents-data.js`。单 agent 对象（首例 `agents-data.js:22-40`）+ 两条派生字段在 `agents-data.js:228-231` 给每条 agent 盖印。

顶层字段（每 agent 对象）:
- `id` (string) — `'agent_01HFK'` (L23)
- `name` (string) — `'论文阅读 · reader-04'` (L23)
- `kind` (string: `claude`/`codex`/`prompt`/`remote`) — `'claude'` (L23)
- `roles` (string[]) — `['reader','analysis']` (L23)
- `status` (string: `running`/`queued`/`idle`/`failed`/`paused`) — `'running'` (L24)
- `availability` (string: `online`/`unstable`/`offline`) — `'online'` (L24)
- `region` (string) — `'ap-northeast'` (L25)
- `daemon` (string) — `'daemon-09'` (L25)
- `run` (string, `R-####` 或 `—`) — `'R-8821'` (L25)
- `flow` (string) — `'flow-batch-reproduce'` (L25)
- `load` (number 0–100) — `78` (L26)
- `cost` (string `'$x.xx'`) — `'$184.20'` (L26)
- `progress` (number 0–100) — `64` (L26)
- `elapsed` (string `'4m12s'`) — `'4m12s'` (L26)
- `inputSchema` (string) — `'{pdf_uri, focus?}'` (L27)
- `outputSchema` (string) — `'{summary, claims[], refs[]}'` (L27)
- `summary` (string) (L28)
- `instructions` (string) (L29)
- `skills` (string[]) — `['arxiv-parse','pdf-extract',…]` (L30)
- `visibility` (string `workspace`/`public`) — `'workspace'` (L31)
- `concurrency` (number) — `4` (L31)
- `model` (string) — `'claude-sonnet-4'` (L31)
- `runtime` (string) — `'claude-code · daemon-09'` (L32)
- `owner` (string) — `'林敏'` (L32)
- `created` (ISO string) — `'2026-05-12T03:20:00Z'` (L33)
- `lastActiveDays` (number) — `0` (L33)
- `activity` (array of `{total:number, ok:number, fail:number}`, 30-day buckets) (L34)
- `logs` (array of `{t:string, l:string, m:string}`) (L35–39)
- `runCount` (number, 派生) — L229
- `failCount` (number, 派生) — L230

### console shape
console 路由是**字面 passthrough** — `apps/console/src/app/api/agents/[id]/route.ts:20-52`。转发到 gateway→dispatch `GET /api/v1/dispatch/agents/:id` 并原样返回 upstream body（L47-51: `const body = await upstream.text(); return new NextResponse(body, ...)`）。真实 shape 由上游 `apps/dispatch/src/routes/agents.ts`（GET `/agents/:id`）产生，返回 `ok(c, { agent, tasks, runs })`——即信封 `{ success, data: { agent, tasks, runs } }`。

dispatch 原始行（snake_case，`apps/dispatch/src/routes/agents.ts`）:
- `agent` (AgentListRow，接口在 `apps/console/src/lib/agents-catalog.ts:114-133` / dispatch `agents.ts:38-57`): `id`, `name`, `kind`, `capability_descriptor`, `executable_path`, `visibility`, `created_at`, `daemon_label`, `daemon_status`, `last_heartbeat_at`, `daemon_capabilities`, `task_id`, `run_id`, `task_status`, `usage`, `duration_ms`, `task_created_at`, `finished_at`
- `tasks` (TaskHistoryRow[], `agents.ts:137-146`): 各 `{ id, run_id, status, usage, duration_ms, created_at, finished_at }`
- `runs` (`agents.ts:146`): 各 `{ id, identifier, status, cost }`

客户端消费（映射后）shape — `apps/console/src/lib/agents-catalog.ts` `fetchAgentDetail` (L401-419) 重映射为 `AgentDetail`（接口 L81-85）:
- `agent` (`CatalogAgent`, L33-58): `id`, `name`, `kind`, `roles`（来自 `capability.tags`）, `status`, `daemon`, `region`, `run`, `load`, `cost`, `latestTaskId`, `latestTaskStatus`, `elapsedMs`, `capability`（`{name?, summary?, inputSchema?, outputSchema?, tags?}`, L61-67）, `createdAt`
- `tasks` (`AgentTaskHistory[]`, L70-78): `{ id, runId, status, usage, durationMs, createdAt, finishedAt }`
- `runs`: `{ id, identifier, status, cost }[]`

### delta

| Field | Design | Console | Status |
|---|---|---|---|
| id | string (L23) | agent.id (L36) | match |
| name | string (L23) | agent.name (L36) | match |
| kind | claude/codex/prompt/remote (L23) | 同 4 + unknown→remote (L25, L152-156) | match |
| roles | 显式 string[] (L23) | 派生自 `capability.tags` (L299) | match（来源不同） |
| status | running/queued/idle/failed/paused (L24) | 同 union + `done`；`paused` 永不可派生 (L203-215, L27-30) | partial-match（`paused` 不可达） |
| availability | online/unstable/offline (L24) | — | **missing-in-console** |
| region | string (L25) | 从 daemon caps 尽力，否则 `—` (L173-188) | match（语义弱） |
| daemon | string (L25) | `daemon_label ?? '—'` (L302) | match |
| run | `R-####` 或 `—` (L25) | `run_id ?? null` (L303) | **type-mismatch**（string `—` vs null） |
| flow | string (L25) | — | **missing-in-console** |
| load | number 0–100 (L26) | 从 elapsed 派生 0–99 (L224-236) | match（派生不同） |
| cost | `'$x.xx'` (L26) | 从 tokens 派生 @ $0.01/1k，`—` fallback (L283-288) | match（语义不同） |
| progress | number 0–100 (L26) | — | **missing-in-console** |
| elapsed | string `'4m12s'` (L26) | `elapsedMs: number\|null` (L308) | **type-mismatch**（string vs ms number） |
| inputSchema | string (L27) | `capability.inputSchema?` (L65) | match（嵌套） |
| outputSchema | string (L27) | `capability.outputSchema?` (L66) | match（嵌套） |
| summary | string (L28) | `capability.summary?` (L64) | match（嵌套） |
| instructions | string (L29) | — | **missing-in-console** |
| skills | string[] (L30) | —（仅 `capability.tags`，复用为 `roles`） | **missing-in-console** |
| visibility | workspace/public (L31) | 原始行有但 `mapRowToCatalogAgent` 丢弃 (L295-311) | **missing-in-console**（映射时丢） |
| concurrency | number (L31) | — | **missing-in-console** |
| model | string (L31) | — | **missing-in-console** |
| runtime | string (L32) | — | **missing-in-console** |
| owner | string (L32) | — | **missing-in-console** |
| created | ISO string (L33) | `createdAt` ISO (L310) | match |
| lastActiveDays | number (L33) | — | **missing-in-console** |
| activity | `[{total,ok,fail}]` 30-day (L34) | —（最接近：`tasks[]` history） | **missing-in-console**（无桶 shape） |
| logs | `[{t,l,m}]` (L35-39) | 不在 detail；独立 `GET /agents/:id/logs` 路由 (`agents-catalog.ts:422-433`) | **missing-in-console**（detail）；sibling 路由可取 |
| runCount | number (L229) | — | **missing-in-console** |
| failCount | number (L230) | — | **missing-in-console** |
| latestTaskId | — | `agent.latestTaskId` (L51) | extra-in-console |
| latestTaskStatus | — | `agent.latestTaskStatus` (L52) | extra-in-console |
| elapsedMs | — | `agent.elapsedMs` (L54) | extra-in-console |
| tasks (history) | — | `AgentTaskHistory[]` (L83) | extra-in-console |
| runs (bound runs) | — | `{id,identifier,status,cost}[]` (L84) | extra-in-console |

**→ 对应里程碑**: M9（后端契约校齐）

---

## 后端契约 2: GET /chatflows/:id

> **[✓ M9.2 @cff3295]** gateway `GET /api/v1/flows/:id` 已落地，返回 design `flows-data` DAG 形状（`apps/gateway/src/flowise-shape.ts` `mapFlowiseToDesignShape`：`{ id, name, version, hash, status, run, nodes:[DesignNode], edges:[{from,to,label?}], runs:[DesignRun] }`）。14 agentflow 节点 `data.name` → design `type` 词汇映射（Start/Agent/LLM/Tool/HTTP/Condition/Condition Agent/Iteration/Loop/Human Input/Direct Reply/Custom Function/Execute Flow/Retriever）；StickyNote 装饰节点过滤。node 字段含 design 完整集（label/sub/x/y/w/h/status/runId/duration/input/output/logs + agent-type extras agent/budget/tokens/cost/timeout），best-effort 从 Flowise `flowData` + executions 填充。验收 `flows-shape.test.ts`（评审修复 `8e121a0`：StickyNote 过滤 + 失败分支测试）。
>
> ⚠️ **消费侧待迁移**：console `/api/flows/:id` 仍走自建 `toFlowDetailView`（`apps/console/src/lib/flows.ts:297-360`，返回 `FlowDetailView` 形状——下方 console shape），未切到 gateway design-shape 路由。下方字段级 delta 表记录**契约形状差异**（design `flows-data` vs console `FlowDetailView`），作为迁移对照——M9.2 已提供目标形状，迁移本身是后续任务。

> 注：design 称「chatflows」或「flows」。

### design shape
源: `design/js/flows-data.js`。单 flow 对象（首例 `flows-data.js:12-39`）。注意 `nodes` 被设两次（L14 为 number `9`，L15 为数组——数组胜出）。

顶层 flow 字段:
- `id` (string) — `'flow_repro_01'` (L13)
- `name` (string) — `'论文批量复现流水线'` (L13)
- `version` (string) — `'v2.3.1'` (L13)
- `hash` (string) — `'7a3f9c'` (L13)
- `status` (string: running/done/queued/failed/paused/idle) — `'running'` (L14)
- `run` (string `R-####`) — `'R-8821'` (L14)
- `nodes` (node 对象数组) (L15-25)
- `edges` (`{from, to, label?}` 数组) (L26-38)

Node shape (L16，首节点；agent 类型节点在 L18 多字段):
- `id`, `type`, `label`, `sub`, `x`, `y`, `w`, `h`, `status`, `runId`(可选), `duration`(可选), `input`, `output`, `logs`(`[{t,l,m}]`)
- agent 类型节点额外: `agent`, `budget`, `tokens`, `cost`, `timeout` (L18)

Edge shape (L27-37): `{ from:string, to:string, label?:string }`

### console shape
路由: `apps/console/src/app/api/flows/[id]/route.ts:21-70`。经 `fetchFlowiseJson` (`apps/console/src/lib/flowise-client.ts:50-90`) 取 chatflow 行 + 近期 executions，再用 `toFlowDetailView` (`apps/console/src/lib/flows.ts:297-360`) 构 `FlowDetailView`，返回 `{ success: true, data: detail }`（路由 L69）。

`FlowDetailView` (`flows.ts:201-224`):
- `id` (string), `name` (string), `type` (string, 默认 `'CHATFLOW'`), `versionHash` (string — **路由未传第 3 参，故默认 `''`**, `flows.ts:300`), `status` (`NodeRunStatus`, L208), `latestExecutionId?` (string, L210), `latestRunId?` (string\|null, L218), `nodes` (`FlowNodeView[]`, L219), `edges` (`FlowEdgeView[]`, L220), `nodeMetrics` (`Record<string, NodeRunMetrics>`, L222), `updatedAt` (ISO string, L223)

`FlowNodeView` (`flows.ts:171-179`): `{ id, label, type, position:{x,y}, status }`
`FlowEdgeView` (`flows.ts:182-188`): `{ id, source, target, label? }`
`NodeRunMetrics` (`flows.ts:191-198`): `{ nodeId, status, executionId?, logs:[{ts,level,msg}] }`

### delta

| Field | Design | Console | Status |
|---|---|---|---|
| id | string (L13) | `flow.id` (L344) | match |
| name | string (L13) | `flow.name` (L345) | match |
| version | `'v2.3.1'` (L13) | —（仅 `versionHash`，本路由恒 `''`） | **missing-in-console** |
| hash | `'7a3f9c'` (L13) | `versionHash` = `''`（路由 L68 调 `toFlowDetailView(flow, execs)` 未传第 3 参） | **missing-in-console**（字段在但恒空） |
| status | running/…/idle (L14) | `mapExecutionState(latest.state)` (L348) | match |
| run | `R-####` (L14) | `latestRunId` = `latest.sessionId` (L354) | type-mismatch（语义：design 当前 run vs console 最新 execution sessionId） |
| nodes (array) | 富 node 对象 (L15-25) | `FlowNodeView[]` (L306-312) | type-mismatch（字段重叠但 node shape 偏离——见下） |
| edges (array) | `{from,to,label?}` (L26-38) | `FlowEdgeView[]` `{id,source,target,label?}` (L314-321) | type-mismatch（重命名键 + 多 id） |
| type | — | `flow.type ?? 'CHATFLOW'` (L346) | extra-in-console |
| latestExecutionId | — | `latest?.id` (L349) | extra-in-console |
| latestRunId | — | `latest?.sessionId ?? null` (L354) | extra-in-console |
| nodeMetrics | — | `Record<string,NodeRunMetrics>` (L326-341) | extra-in-console |
| updatedAt | — | `toIso(flow.updatedDate)` (L358) | extra-in-console |

Node 级 delta (design node vs `FlowNodeView` + `NodeRunMetrics`):

| Node field | Design | Console | Status |
|---|---|---|---|
| id | L16 | `n.id` (L307) | match |
| type | L16 | `n.type ?? 'customNode'` (L309) | match |
| label | L16 | `n.data?.label \|\| n.id` (L308) | match |
| sub | L16 | — | **missing-in-console** |
| x / y | L16（分开） | `position.x` / `position.y` (L310) | match（重构） |
| w / h | L16 | — | **missing-in-console** |
| status | L16 | `statusByNode[n.id] ?? 'idle'` (L311) | match |
| runId | L16（每节点） | —（仅 flow 级 `latestRunId`） | **missing-in-console** |
| duration | L16 | — | **missing-in-console** |
| input | L16 | — | **missing-in-console** |
| output | L16 | — | **missing-in-console** |
| logs | `[{t,l,m}]` 每节点 (L16) | `nodeMetrics[nodeId].logs`——尽力单行 (L393-421) | partial（console 退化到 ≤1 行） |
| agent | L18 | — | **missing-in-console** |
| budget | L18 | — | **missing-in-console** |
| tokens | L18 | — | **missing-in-console** |
| cost | L18 | — | **missing-in-console** |
| timeout | L18 | — | **missing-in-console** |

Edge 级 delta:

| Edge field | Design | Console | Status |
|---|---|---|---|
| from | L27 | `source` (L317) | match（重命名） |
| to | L27 | `target` (L318) | match（重命名） |
| label? | L28 | `e.label ?? e.data?.label` (L320) | match |
| id | — | `e.id ?? 'e-…-…-i'` (L316) | extra-in-console |

**→ 对应里程碑**: M9（后端契约校齐）

---

## 后端契约 3: POST /tasks

> **[✓ M9.3 @cbd425b]** gateway `POST /api/v1/tasks` 已落地（`apps/gateway/src/routes/tasks.ts`），从零实现 plan v0.3-M9.3 契约（非校齐——design `new-task.html` 本身不 POST，见下方 design shape）。zod 校验 design 提交体（task/dir/flows/agents/contextRefs/priority/dueDate + assigneeType），落库 `tasks` 行 + `runs` 占位行（status=`pending`，path 按 assigneeType），返回 `{ task:{id,status,runId}, runId, path }`。双路径：`assigneeType='flow'` → path=`flow`（Path A）；`'agent'|'squad'` → path=`direct`（Path B）。MVP 不触发真实 dispatch（仅落库 + 返回 path）。companion migration `1720000008002`（tasks 表 + runs.task_id/agent_id/path 列）。验收 `tasks-contract.test.ts`（11 用例）。
>
> ⚠️ **消费侧待迁移**：console `/tasks/new` 的 `doSend` 仍 `router.push('/workspace?new=1&…')`（移植 design 的 GET 导航，`new-task-view.tsx:365-383`），未切到 POST `/api/v1/tasks`；`/workspace` 页也不消费 `?new=1` query。下方 delta 表记录**契约形状差异**（design GET 导航 vs console 当前 + plan 规定的 POST 形状），作为迁移对照——M9.3 已提供 POST 端点，消费侧迁移是后续任务。

### design shape
源: `design/new-task.html`，`doSend()` 函数 (`design/new-task.html:564-574`)。状态持有 `state = { dir, flows, agents }` (L401-405)。

design **不 POST**。它通过 `location.href` 做客户端页面跳转:
```
location.href = 'workspace.html?new=1&' + params.toString();   // L573
```
- **Endpoint URL**: `workspace.html?new=1&...`（页面，非 API）
- **HTTP method**: `GET`（浏览器导航）——无 fetch/XHR
- **「Request body」**（query params, L567-572）:
  - `task` (string, required — textarea 文本) L568
  - `dir` (string, optional — `state.dir.path`) L569
  - `flows` (逗号分隔 flow ids, optional — `state.flows.join(',')`) L570
  - `agents` (逗号分隔 agent ids, optional — `state.agents.join(',')`) L571
  - `new=1` flag（恒设） L573
- **Expected response**: 无——是导航；workspace 页应读这些 params 并种新 task 线程。

### console shape
**`POST /tasks` 不存在。** 确证：`find apps/console/src/app/api -name route.ts` 列出全部路由；无 `tasks/route.ts` 或 `tasks/[id]/route.ts`。grep `tasks` 在 console API 层仅命中无关 test/agent-catalogue。

console 也**无 `new-task` 页**（`apps/console/src/app/**` 无 `new-task` 路由段——pages 仅 dashboard/agents/flows/lab/workspace/settings/login）。交接目标 `workspace.html?new=1&...` 原样不存在，console 的 `apps/console/src/app/workspace/page.tsx` + `components/workspace-view.tsx` **不**读 `?new=1`、`task`、`dir`、`flows`、`agents` query params（grep 零命中）。故当前 console 里 design 的 `doSend()` 导航会落在 workspace 页但其 query params 被静默忽略。

console 真正的 task-creation 面是**完全不同的传输**：`POST /api/workspaces/[id]/runs` (`apps/console/src/app/api/workspaces/[id]/runs/route.ts:40-100`)，转发到 scheduler `POST /api/v1/scheduler/runs/fanout`（路由 L67）。由 workspace composer 调用 (`apps/console/src/components/workspace-view.tsx:231-285`，`buildWorkspaceRunBody` 调用在 L266，`postWorkspaceRun` 在 L273)。

console request body (`buildWorkspaceRunBody`, `apps/console/src/lib/workspaces.ts:338-371`):
- `flowId` (string) — L355
- `pipelineId` (string, 镜像 flowId) — L358
- `identifier` (string) — L359
- `inputs` (array, 一项) — L360-368:
  - `body`: `{ question: string, streaming: boolean, overrideConfig: { sessionId: string } }`

console response (`postWorkspaceRun`, `apps/console/src/lib/workspaces.ts:493-511`): 解包 scheduler 信封 `{ success, data: { parentRunId: string } }` 返回 `{ runId: string }` 给客户端。

### delta

| Aspect | Design | Console | Status |
|---|---|---|---|
| endpoint | `workspace.html?new=1`（页导航） | `POST /api/workspaces/[id]/runs`（路由 L40-100） | **mismatch**（不同传输；无 `POST /tasks`） |
| HTTP method | GET（导航） | POST（JSON） | **mismatch** |
| request: task 文本 | query `task` (string, L568) | `inputs[0].body.question` (string, L362) | **mismatch**（重命名 + 深嵌套） |
| request: directory | query `dir` (string, L569) | — | **missing-in-console** |
| request: flows | query `flows` (csv, 多选, L570) | `flowId`（单选——项目首个 linked flow，`workspace-view.tsx:240`/`:267`） | **mismatch**（design 多选 vs console 单选硬编码 flow） |
| request: agents | query `agents` (csv, L571) | — | **missing-in-console** |
| request: new flag | `new=1` (L573) | — | **missing-in-console** |
| request: pipelineId | — | `pipelineId`（= flowId, L358） | extra-in-console |
| request: identifier | — | `identifier` (L359) | extra-in-console |
| request: streaming | — | `streaming: false` (L363) | extra-in-console |
| request: sessionId | — | `overrideConfig.sessionId` (L365) | extra-in-console |
| request: workspaceId | — | `workspaceId`（由路由 L62 从 URL 注入） | extra-in-console |
| response | 无（导航） | `{ success, data: { parentRunId } }` → 客户端 `{ runId }` (L503-511) | **mismatch**（design 无 response；console 返回 run id） |

**小结（契约 3）**: `POST /tasks` 在 console 不存在。design 的 `new-task.html` 也不 POST——它 GET 导航到 `workspace.html?new=1&task=&dir=&flows=&agents=`。console 无 new-task 页、workspace 页无 `?new=1` 消费者，task 创建走 `POST /api/workspaces/:id/runs`（scheduler fan-out）。design 的 `dir`、`flows`（多）、`agents` 关联在 console 全无对应——console composer 只对项目首个 linked flow 发单条 `question`。

**→ 对应里程碑**: M3（new-task）+ M9（后端契约校齐）

---

## 附录：里程碑 → 审计小节映射

| 里程碑 | 任务 | 对应审计小节 |
|---|---|---|
| M0.1 | 写 design-fidelity 审计文档（本文） | 全文 |
| 里程碑 | 任务 | 对应审计小节 | 落地状态 |
|---|---|---|---|
| M0.1 | 写 design-fidelity 审计文档（本文） | 全文 | [✓ @1301bbf] |
| M0.2 | UI 组件测试基建 | — | [✓ @653ab39] |
| M0.3 | Flowise iframe env | — | [✓ @708a1be] |
| M1.1 | shell 交互对齐 design | §5 index（部分） | [✓ @9c5a940] |
| M2.1 | agentflows list-page 保真 | §1 agentflows | [✓ @11997412] |
| M2.2 | agentflows list→detail + DAG + inspector io-box | §1 agentflows | [✓ @3a0c24f] |
| M2.3 | /flows/[id]/edit iframe 嵌入 Flowise 画布 | §1 agentflows | [✓ @3b38f24] |
| M3.1 | new-task picker 模态 | §2 new-task | [✓ @5e1c6f7] |
| M3.2 | new-task ⏎发送/⇧⏎换行 + IME 守卫 | §2 new-task | [✓ @47927d1] |
| M3.3 | new-task 目录卡 contextRefs | §2 new-task | [✓ @4f6e093] |
| M4.1 | agent-detail 两栏 + 30 桶 activity sparkline | §3 agent-detail | [✓ @ac2927f] |
| M4.2 | agent-detail WS live status | §3 agent-detail | [✓ @426d703]（hub 端点待接，见 §10） |
| M5.1 | agents 列表 scope tabs + filter chips + 排序 + result-count | §4 agents | [✓ @8fe1164]（评审修复 @3a9d39a） |
| M6.1 | index launcher hero CTA + arch-strip | §5 index | [✓ @168b22e] |
| M7.1 | workspace filter chips + ws-meta 关联 flow 卡 | §6 workspace | [✓ @cd520b9]（评审修复 @a0d0360） |
| M7.x | lab sessions + 线程化 messages + @mention + thinking/tool-card | §7 lab | [✓ @64686ff] |
| M8.1 | dashboard 保真（density/donut/throughput/usage/时间窗） | §8 dashboard | [✓ @524aa2d] |
| M8.2 | settings 6 tab 形状对齐 design | §9 settings | [✓ @b43bfc8] |
| M9.1 | GET /agents/:id 响应对齐 design agents-data.js | 契约 1 | [✓ @530b3db]（评审修复 @525dd3e） |
| M9.2 | GET /api/v1/flows/:id 返回 flows-data DAG 形状 | 契约 2 | [✓ @cff3295]（评审修复 @8e121a0） |
| M9.3 | POST /api/v1/tasks 创建路由（双路径） | 契约 3 | [✓ @cbd425b] |
| M10 | e2e + viewport 矩阵 | — | post-MVP |
| M11.1 | 收口（本文回填 + architecture §10 状态） | 全文 + §10 | [✓ 本提交] |

每任务完成后回本文对应小节标 ✓（在里程碑行或小节标题加 `[✓ Mx.y @commit-sha]`）。

---

## §10 控制台 9 屏保真状态（v0.3 收口）

> 本节替代上游 plan 所指 `docs/architecture-v0.3.md §10.2` route 表——该文件在仓库中从未建稿（见文首收口说明）。这里给出 9 屏 + 后端契约的**已保真实现状态总览**，作为 v0.3 design-fidelity 收口的架构落点。对照基线：main HEAD `4196965`（2026-07-14）。

### §10.1 9 屏路由表（已保真实现）

| # | 屏 (design file) | console 路由 | 主组件 | 保真度 | 落地 commit |
|---|---|---|---|---|---|
| 1 | agentflows (`agentflows.html`) | `/flows`, `/flows/[id]/edit` | `flows-view.tsx` + `flow-dag.tsx` + `dag-node.tsx` | list↔detail + DAG + inspector io-box + hash deep-link + edit-iframe；URL hash/DAG 图标见 §1.x | M2.1 `@11997412` / M2.2 `@3a0c24f` / M2.3 `@3b38f24` |
| 2 | new-task (`new-task.html`) | `/tasks/new` | `new-task-view.tsx` | picker 模态 + 目录卡 + ⏎⇧⏎ + IME 守卫 + 模板；`doSend` GET 导航（POST 端点已就绪，消费侧待迁移） | M3.1 `@5e1c6f7` / M3.2 `@47927d1` / M3.3 `@4f6e093` |
| 3 | agent-detail (`agent-detail.html`) | `/agents/[id]` | `agent-detail-view.tsx` + `agent-activity-sparkline.tsx` | 两栏 split + 4 tabs（Activity/Instructions/Skills/Logs）+ 30 桶 activity + WS live status（hub 端点待接，断线轮询 fallback） | M4.1 `@ac2927f` / M4.2 `@426d703` |
| 4 | agents (`agents.html`) | `/agents` | `agents-view.tsx` | scope tabs + filter chips（单选）+ 排序 + result-count；多选下拉/批操作/toast/骨架屏 post-MVP | M5.1 `@8fe1164`（修复 `@3a9d39a`） |
| 5 | index (`index.html`) | `/` (launcher), `/chat` | `launcher-view.tsx` + `lib/launcher.ts` | hero CTA + 4 KPI stats + arch-strip 7 层（design 4 步扩展）；module-grid post-MVP | M6.1 `@168b22e` |
| 6 | workspace (`workspace.html`) | `/workspace` | `workspace-view.tsx` | 4 filter chips（真过滤）+ ws-meta 关联 flow 卡 + quota + artifacts；`?new=1` 消费 + 项目入 sidebar post-MVP | M7.1 `@cd520b9`（修复 `@a0d0360`） |
| 7 | lab (`lab.html`) | `/lab` | `lab-view.tsx` | sessions + 线程化 messages + @mention（含内联着色）+ thinking/tool-card + mode switch（持久化）+ 归档；假设/数据/agent 自主回环 post-MVP | M7.x `@64686ff` |
| 8 | dashboard (`dashboard.html`) | `/dashboard` | `dashboard-view.tsx` | density（任务队列马赛克）+ donut（daemon 态）+ throughput（2×2 KPI）+ usage（按模型）+ 3 窗 toggle；1M canvas/面积图/Provider 分摊 post-MVP | M8.1 `@524aa2d` |
| 9 | settings (`settings.html`) | `/settings` | `settings-view.tsx` | 6 tab 4 组形状对齐；API Key live CRUD；tab 2–6 形状对齐 + design 占位数据（数据接线 post-MVP） | M8.2 `@b43bfc8` |

### §10.2 后端契约保真状态

| 契约 | 端点 | 实现位置 | design 形状对齐 | 消费侧 |
|---|---|---|---|---|
| 1 | `GET /api/v1/agents/:id` | `apps/gateway/src/routes/agents.ts` (`toAgentDto`) | [✓ M9.1] 1:1 映射 design `agents-data.js` 字段集 | ⚠️ console 仍走 dispatch 代理 `/api/v1/dispatch/agents/:id`（snake_case），迁移到 gateway design 路由是 M5 follow-up |
| 2 | `GET /api/v1/flows/:id` | `apps/gateway/src/flowise-shape.ts` (`mapFlowiseToDesignShape`) | [✓ M9.2] 返回 design `flows-data` DAG（14 节点类型映射，StickyNote 过滤） | ⚠️ console `/api/flows/:id` 仍用自建 `toFlowDetailView`，迁移到 gateway design 路由是后续任务 |
| 3 | `POST /api/v1/tasks` | `apps/gateway/src/routes/tasks.ts` | [✓ M9.3] design 提交体 + 双路径（flow→Path A / agent|squad→Path B）+ runId 落库 | ⚠️ console `/tasks/new` 仍 GET 导航 `/workspace?new=1`，切到 POST + `/workspace` 消费 `?new=1` 是后续任务 |

### §10.3 §6.8 WS hub 状态（M4.2 补注）

`packages/contracts/src/ws.ts` 定义 `ConsoleWsFrame`（`agent-updated` / `run-updated`）+ `apps/console/src/lib/ws-client.ts` 单例 socket（指数退避重连，断线时视图轮询 fallback）。`wsUrl()` 默认 `ws://localhost:8080/ws`（`NEXT_PUBLIC_WS_URL` 可覆盖）。**注意**：gateway 当前**尚未挂 `/ws` WebSocket 端点**——M4.2 落地了客户端 + 契约 + 轮询 fallback，但 hub 服务端 emit 端点是后续任务（agent-detail 在 hub 缺席时降级为轮询，功能不中断）。这是 v0.3 收口时**唯一已知「客户端就绪、服务端待接」**的缺口。

### §10.4 post-MVP 遗留（有意推迟，非遗漏）

以下为各屏明确标注 ⬜ post-MVP 的项，依赖平台尚未提供的 API 或超出 MVP 边界，**不在 v0.3 保真收口范围**：
- agents：多选下拉 filter + 每值计数、批量多选 + 批操作栏、行 ⋯ popover、30 天 sparkline 列、运行次数列、搜索清空 ×、骨架屏、toast、空态增强、注册 Agent 操作
- index：module grid + section labels
- workspace：项目入 sidebar、`?new=1` 消费、chat-head 今日消息数、page actions（归档/新建）
- lab：artifacts 假设/数据组、agent 自主回环、sessions-head + 新建 icon、新实验完整配置
- dashboard：1M-agent canvas 热图、24h 面积图、Provider 成本分摊、事件流卡、导出 CSV、刷新动画、30d 窗
- settings：tab 2–6 数据接线（默认模型/预算熔断/通知/账户团队/危险区 live）、gateway 卡细节、copy/reveal（安全上无原文）、revoke checkbox、topbar 搜索、toast 动画
- 契约消费侧迁移：console agents/flows/tasks 从 dispatch/自建形状切到 gateway design-shape 路由（M5 follow-up）
- WS hub 服务端端点（§10.3）

---

_审计基线 commit: `710b863` (main HEAD, feat: new design)_
_审计分支: `issue/MZW-286`_
_收口 commit: 本提交（v0.3-M11.1，基于 main `4196965`）_
