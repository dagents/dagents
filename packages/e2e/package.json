{
  "name": "@dagents/e2e",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "echo 'no build step — vitest transforms TS source in place'",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@hono/node-server": "^1.12.0",
    "@dagents/contracts": "workspace:*",
    "@dagents/agent-adapters": "workspace:*",
    "@dagents/daemon": "workspace:*",
    "@dagents/db": "workspace:*",
    "@dagents/shared": "workspace:*"
  },
  "devDependencies": {
    "@opentelemetry/api": "^1.9.0",
    "@opentelemetry/core": "^1.29.0",
    "@types/node": "^20.0.0",
    "tsx": "^4.0.0",
    "typescript": "^5.5.0",
    "vitest": "^2.0.0"
  }
}
